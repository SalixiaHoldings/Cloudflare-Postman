# Official v3 migration preserves significant whitespace in YAML block scalars.
# Byte-for-byte generation checks enforce these files; never trim migrated output.
postman/**/*.yaml -text whitespace=-blank-at-eol,-space-before-tab
