$kind: http-example
id: bc4dd1c7-319a-5d59-81a4-a93995bfd8d8
name: Failure response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/rum/site_info/{{site_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
