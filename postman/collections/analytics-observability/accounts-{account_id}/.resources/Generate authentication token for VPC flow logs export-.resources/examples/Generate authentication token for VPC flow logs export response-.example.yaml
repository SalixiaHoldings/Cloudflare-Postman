$kind: http-example
id: 374e5ff3-20a1-5c1b-aea0-be2587e581d4
name: Generate authentication token for VPC flow logs export response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/mnm/vpc-flows/token"
  method: POST
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
