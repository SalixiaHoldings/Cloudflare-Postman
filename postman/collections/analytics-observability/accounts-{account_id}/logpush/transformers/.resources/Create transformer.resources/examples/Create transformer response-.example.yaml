$kind: http-example
id: 24633d33-7ee0-5981-81e5-a2711a6510ed
name: Create transformer response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/logpush/transformers"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "name": "redact-pii",
        "code": "SELECT ClientIP, RayID FROM http_requests",
        "description": "Redacts PII fields from HTTP request logs."
      }
response:
  statusCode: 201
  statusText: Created
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
