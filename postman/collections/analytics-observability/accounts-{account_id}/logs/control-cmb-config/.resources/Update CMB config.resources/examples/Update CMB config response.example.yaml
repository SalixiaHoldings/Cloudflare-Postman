$kind: http-example
id: 213342e8-b3ae-51d3-8d35-d1e1c4a53b87
request:
  url: "{{base_url}}/accounts/{{account_id}}/logs/control/cmb/config"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "allow_out_of_region_access": false,
        "regions": "eu"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
