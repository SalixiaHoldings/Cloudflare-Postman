$kind: http-example
id: 5f93ecc6-49e7-5167-8725-4a05b4b19d7b
request:
  url: "{{base_url}}/accounts/{{account_id}}/logs/control/cmb/config"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "allow_out_of_region_access": false,
        "regions": "eu"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
