$kind: http-example
id: f39e4f2f-b1dd-5907-8204-84607056dc32
name: Patch Health Check response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/smart_shield/healthchecks/{{healthcheck_id}}"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "name": "server-1",
        "address": "www.example.com",
        "check_regions": [
          "WEU",
          "ENAM"
        ],
        "consecutive_fails": 1,
        "consecutive_successes": 1,
        "description": "Health check for www.example.com",
        "interval": 60,
        "retries": 2,
        "suspended": false,
        "timeout": 5,
        "type": "HTTPS"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
