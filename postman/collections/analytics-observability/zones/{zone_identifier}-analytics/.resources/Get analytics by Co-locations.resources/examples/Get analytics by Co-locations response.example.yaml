$kind: http-example
id: fcd7c2c7-4c90-5bba-a5be-e877f150ff6c
request:
  url: "{{base_url}}/zones/{{zone_identifier}}/analytics/colos"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: until
      value: <string>
      disabled: true
    - key: since
      value: string
      disabled: true
    - key: continuous
      value: "true"
      disabled: true
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "query": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        },
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
