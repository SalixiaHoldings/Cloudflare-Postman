$kind: http-example
id: 31cd996a-1375-5b25-a0be-9295284e6e76
name: Risk events.
request:
  url: "{{base_url}}/accounts/{{account_id}}/zt_risk_scoring/{{user_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
