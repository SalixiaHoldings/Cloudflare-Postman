$kind: http-example
id: b5574aa7-b1fa-5975-a277-6557ecf04a74
name: Risk scoring behaviors.
request:
  url: "{{base_url}}/accounts/{{account_id}}/zt_risk_scoring/behaviors"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
