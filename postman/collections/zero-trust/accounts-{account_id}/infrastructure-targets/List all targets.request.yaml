$kind: http-request
id: 816121c4-52b8-5c0c-9a71-6833a92a8ac1
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: infra-targets-list
  GET /accounts/{account_id}/infrastructure/targets

  Lists and sorts an account's targets. Filters are optional and are ANDed
  together.

  Authentication support: multi-scheme-or-other
  Auth declaration SHA-256: cb039fe92c74f6483aaad6a3be8d90dfc474320bb13b733f4b11d38e19a5507f
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[],"api_token":[]}]
  Selected requirement: {"api_email":[],"api_key":[],"api_token":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/infrastructure/targets"
method: GET
headers:
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
queryParams:
  - key: hostname
    value: string
    disabled: true
    description: Hostname of a target
  - key: hostname_contains
    value: string
    disabled: true
    description: Partial match to the hostname of a target
  - key: virtual_network_id
    value: f4709510-89e8-3308-9f09-1c3185c4c980
    disabled: true
    description: Private virtual network identifier of the target
  - key: ip_v4
    value: string
    disabled: true
    description: IPv4 address of the target
  - key: ip_v6
    value: string
    disabled: true
    description: IPv6 address of the target
  - key: created_before
    value: 1991-02-22T17:32:24.346Z
    disabled: true
    description: Date and time at which the target was created before (inclusive)
  - key: created_after
    value: 1991-02-22T17:32:24.346Z
    disabled: true
    description: Date and time at which the target was created after (inclusive)
  - key: modified_before
    value: 1991-02-22T17:32:24.346Z
    disabled: true
    description: Date and time at which the target was modified before (inclusive)
  - key: modified_after
    value: 1991-02-22T17:32:24.346Z
    disabled: true
    description: Date and time at which the target was modified after (inclusive)
  - key: ips
    value: string
    disabled: true
    description: |-
      Filters for targets that have any of the following IP addresses. Specify
      `ips` multiple times in query parameter to build list of candidates.
  - key: ips
    value: string
    disabled: true
    description: |-
      Filters for targets that have any of the following IP addresses. Specify
      `ips` multiple times in query parameter to build list of candidates.
  - key: target_ids
    value: 2b8d7107-93ff-8d38-e62f-6214df5c1d47
    disabled: true
    description: |-
      Filters for targets that have any of the following UUIDs. Specify
      `target_ids` multiple times in query parameter to build list of
      candidates.
  - key: target_ids
    value: cd75d569-4978-454a-1390-f471c6f8d74a
    disabled: true
    description: |-
      Filters for targets that have any of the following UUIDs. Specify
      `target_ids` multiple times in query parameter to build list of
      candidates.
  - key: ip_like
    value: string
    disabled: true
    description: |-
      Filters for targets whose IP addresses look like the specified string.
      Supports `*` as a wildcard character
  - key: ipv4_start
    value: string
    disabled: true
    description: |-
      Defines an IPv4 filter range's starting value (inclusive). Requires
      `ipv4_end` to be specified as well.
  - key: ipv4_end
    value: string
    disabled: true
    description: |-
      Defines an IPv4 filter range's ending value (inclusive). Requires
      `ipv4_start` to be specified as well.
  - key: ipv6_start
    value: string
    disabled: true
    description: |-
      Defines an IPv6 filter range's starting value (inclusive). Requires
      `ipv6_end` to be specified as well.
  - key: ipv6_end
    value: string
    disabled: true
    description: |-
      Defines an IPv6 filter range's ending value (inclusive). Requires
      `ipv6_start` to be specified as well.
  - key: tag
    value: string
    disabled: true
    description: |-
      Filter by tag key:value pairs. Multiple `tag` params are AND'd.
      Format: `tag=key:value` (e.g., `tag=environment:production`).
      Key and value must both be non-empty; `tag=:value` and `tag=key:` return 400.
  - key: tag
    value: string
    disabled: true
    description: |-
      Filter by tag key:value pairs. Multiple `tag` params are AND'd.
      Format: `tag=key:value` (e.g., `tag=environment:production`).
      Key and value must both be non-empty; `tag=:value` and `tag=key:` return 400.
  - key: page
    value: "1"
    disabled: true
    description: Current page in the response
  - key: per_page
    value: "1000"
    disabled: true
    description: Max amount of entries returned per page
  - key: order
    value: created_at
    disabled: true
    description: The field to sort by.
  - key: direction
    value: desc
    disabled: true
    description: The sorting direction.
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/List all targets.resources/examples
order: 1000
