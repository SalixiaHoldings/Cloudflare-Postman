$kind: http-example
id: affa8c6e-0078-5477-97a3-b06d614e9ee8
name: Success response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dex/rules"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "name": "string",
        "match": "string",
        "description": "string"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
