$kind: http-request
id: d8f3d96b-437c-543d-a235-65832c3551b6
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: device-dex-test-details
  GET /accounts/{account_id}/dex/devices/dex_tests

  Fetch all DEX tests.

  Authentication support: legacy-only
  Auth declaration SHA-256: a9720528d5568ee0454de8ee346c99dd84fa50a430a5033cc320160dae1660d9
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[]}]
  Selected requirement: {"api_email":[],"api_key":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/dex/devices/dex_tests"
method: GET
headers:
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
queryParams:
  - key: page
    value: "1"
    disabled: true
    description: Page number of paginated results.
  - key: per_page
    value: "10"
    disabled: true
    description: Number of results per page.
  - key: testName
    value: string
    disabled: true
    description: Filter by test name.
  - key: kind
    value: traceroute
    disabled: true
    description: Filter by test type.
auth:
  type: noauth
examples: ./.resources/List Device DEX tests.resources/examples
order: 1000
