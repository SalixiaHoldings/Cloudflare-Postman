$kind: http-example
id: 58fdaede-2a55-5a6f-871a-355db2e3cfdc
name: Device DEX test response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dex/devices/dex_tests/{{dex_test_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
