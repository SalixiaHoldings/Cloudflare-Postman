$kind: http-request
id: 22031313-a36d-5c40-b3f2-16b1f522f2dc
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: ip-access-rules-for-an-account-list-ip-access-rules
  GET /accounts/{account_id}/firewall/access_rules/rules

  Fetches IP Access rules of an account. These rules apply to all the zones in the account. You can filter the results using several optional parameters.

  Authentication support: multi-scheme-or-other
  Auth declaration SHA-256: cb039fe92c74f6483aaad6a3be8d90dfc474320bb13b733f4b11d38e19a5507f
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[],"api_token":[]}]
  Selected requirement: {"api_email":[],"api_key":[],"api_token":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/firewall/access_rules/rules?mode=challenge&configuration.target=ip&configuration.value=198.51.100.4&notes=my note&match=all&page=1&per_page=20&order=mode&direction=desc"
method: GET
headers:
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
queryParams:
  mode: challenge
  configuration.target: ip
  configuration.value: 198.51.100.4
  notes: my note
  match: all
  page: "1"
  per_page: "20"
  order: mode
  direction: desc
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/List IP Access rules.resources/examples
order: 1000
