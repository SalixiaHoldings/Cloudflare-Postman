$kind: http-example
id: cffda26f-07a6-5ef5-b806-ee4c051ffcef
name: Get an IP Access rule response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/firewall/access_rules/rules/{{rule_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
