$kind: http-request
id: d1173ea1-dd28-50e7-86c0-c5731ed62372
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: ip-access-rules-for-an-account-create-an-ip-access-rule
  POST /accounts/{account_id}/firewall/access_rules/rules

  Creates a new IP Access rule for an account. The rule will apply to all zones in the account.

  Note: To create an IP Access rule that applies to a single zone, refer to the [IP Access rules for a zone](#ip-access-rules-for-a-zone) endpoints.

  Authentication support: multi-scheme-or-other
  Auth declaration SHA-256: cb039fe92c74f6483aaad6a3be8d90dfc474320bb13b733f4b11d38e19a5507f
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[],"api_token":[]}]
  Selected requirement: {"api_email":[],"api_key":[],"api_token":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/firewall/access_rules/rules"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
body:
  type: json
  content: |-
    {
      "mode": "challenge",
      "configuration": {},
      "notes": ""
    }
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/Create an IP Access rule.resources/examples
order: 2000
