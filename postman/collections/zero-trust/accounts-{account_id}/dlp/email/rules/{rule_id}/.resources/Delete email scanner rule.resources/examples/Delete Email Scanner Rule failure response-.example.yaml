$kind: http-example
id: d7f9b1ae-4cf4-5f27-b829-8eb1e3963a60
name: Delete Email Scanner Rule failure response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/email/rules/{{rule_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
