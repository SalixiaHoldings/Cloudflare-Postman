$kind: http-example
id: ceceaf27-533e-5c50-9c3e-7a3f010dc9bb
name: Update Email Scanner Rule response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/email/rules/{{rule_id}}"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "name": "string",
        "enabled": true,
        "conditions": [
          {
            "value": [],
            "selector": "Recipients",
            "operator": "InList"
          },
          {
            "value": [],
            "selector": "Recipients",
            "operator": "InList"
          }
        ],
        "action": {
          "action": "Block"
        },
        "description": "string"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
