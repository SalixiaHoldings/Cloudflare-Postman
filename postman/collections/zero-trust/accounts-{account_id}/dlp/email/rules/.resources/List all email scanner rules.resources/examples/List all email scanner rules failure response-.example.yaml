$kind: http-example
id: 6e40fcc9-73d1-5bdb-b8f0-7921d36bce14
name: List all email scanner rules failure response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/email/rules"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
