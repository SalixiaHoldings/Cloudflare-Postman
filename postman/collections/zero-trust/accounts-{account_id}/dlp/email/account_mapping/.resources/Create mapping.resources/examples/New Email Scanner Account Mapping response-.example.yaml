$kind: http-example
id: d0f2e257-6c97-58e1-b26d-bc9fbcdc3ea2
name: New Email Scanner Account Mapping response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/email/account_mapping"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "auth_requirements": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
