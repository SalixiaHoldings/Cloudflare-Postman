$kind: http-example
id: 837da769-0589-576f-a615-6adba4181c77
name: Get Email Scanner Account Mapping failure response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/email/account_mapping"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
