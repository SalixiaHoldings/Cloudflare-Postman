$kind: http-example
id: f08a935b-8b74-5018-aa1b-6456606678a9
name: DLP settings reset.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/settings"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
