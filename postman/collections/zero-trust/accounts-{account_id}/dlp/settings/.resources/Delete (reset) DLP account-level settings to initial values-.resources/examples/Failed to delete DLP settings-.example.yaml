$kind: http-example
id: f80ab715-9cf1-5c75-bcb6-fdb62628b031
name: Failed to delete DLP settings.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/settings"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
