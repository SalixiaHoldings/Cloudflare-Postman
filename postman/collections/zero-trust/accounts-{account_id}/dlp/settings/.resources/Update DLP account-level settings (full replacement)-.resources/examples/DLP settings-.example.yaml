$kind: http-example
id: 2cecbda7-cf6f-565d-8a7e-bd3751d65dfb
name: DLP settings.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/settings"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "ai_context_analysis": false,
        "ocr": false,
        "payload_logging": {
          "x-stainless-terraform-configurability": "computed_optional",
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
