$kind: http-example
id: dfe1bde5-fda1-5197-9ee8-184211dc4f89
name: Failed to validate.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/patterns/validate"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "regex": "string",
        "max_match_bytes": 5588
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
