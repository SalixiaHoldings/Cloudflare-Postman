$kind: http-example
id: 417e1d9c-b3da-599c-acdb-133bd36946aa
name: Validation response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/patterns/validate"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "regex": "string",
        "max_match_bytes": 934
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
