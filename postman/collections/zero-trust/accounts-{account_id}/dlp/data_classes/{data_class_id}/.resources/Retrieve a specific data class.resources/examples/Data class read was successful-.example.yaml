$kind: http-example
id: d71bebc3-691c-5db1-a88d-a0d86016c3f2
name: Data class read was successful.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/data_classes/{{data_class_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
