$kind: http-example
id: 338fb74e-b57a-5e73-aefa-4ed467118b6f
name: Data class read failed.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/data_classes/{{data_class_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
