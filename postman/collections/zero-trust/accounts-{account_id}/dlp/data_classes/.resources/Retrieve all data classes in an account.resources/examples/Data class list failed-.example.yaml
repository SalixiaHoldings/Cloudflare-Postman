$kind: http-example
id: 392b97aa-6e44-5b31-ba4d-7a7ab879caae
name: Data class list failed.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/data_classes"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
