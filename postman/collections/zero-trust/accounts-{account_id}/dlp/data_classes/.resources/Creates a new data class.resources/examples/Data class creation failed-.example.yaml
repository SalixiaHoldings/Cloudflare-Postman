$kind: http-example
id: 451cf2ba-1cf2-59bb-ae0e-f7c69b485375
name: Data class creation failed.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/data_classes"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "name": "string",
        "expression": "string",
        "sensitivity_levels": [
          {
            "group_id": "{{sensitivity_levels__0__group_id}}",
            "level_id": "{{sensitivity_levels__0__level_id}}"
          },
          {
            "group_id": "{{sensitivity_levels__1__group_id}}",
            "level_id": "{{sensitivity_levels__1__level_id}}"
          }
        ],
        "data_tags": [
          "68c03d3a-f838-02df-0a4f-1c9e4737599f",
          "ca8e9a0e-3c11-3325-8f7f-e1d9649c5e9b"
        ],
        "description": "string"
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
