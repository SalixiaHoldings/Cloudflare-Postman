$kind: http-example
id: 07b6c79d-a714-5419-b578-3b0e75cb3971
name: Data tag category delete failed.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/data_tag_categories/{{category_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
