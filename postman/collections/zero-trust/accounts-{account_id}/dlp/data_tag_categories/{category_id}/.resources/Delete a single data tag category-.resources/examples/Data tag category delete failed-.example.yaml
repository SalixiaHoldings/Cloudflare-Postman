$kind: http-example
id: 3e5e4350-90cc-51b6-b034-ae215eb03645
name: Data tag category delete failed.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/data_tag_categories/{{category_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
