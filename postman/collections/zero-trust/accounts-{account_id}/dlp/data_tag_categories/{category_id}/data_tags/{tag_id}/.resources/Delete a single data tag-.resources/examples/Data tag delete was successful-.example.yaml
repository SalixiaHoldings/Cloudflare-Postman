$kind: http-example
id: 2f1a6461-482c-5b40-b782-1d5e64e51daf
name: Data tag delete was successful.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/data_tag_categories/{{category_id}}/data_tags/{{tag_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
