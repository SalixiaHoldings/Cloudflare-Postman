$kind: http-example
id: dc0c570e-c610-5ab3-b60c-ed8b5f25e00e
name: Data tag delete was successful.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/data_tag_categories/{{category_id}}/data_tags/{{tag_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
