$kind: http-example
id: cc3fe905-8186-5244-9cc4-d681d397ed39
name: Get entry response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/entries/{{entry_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
