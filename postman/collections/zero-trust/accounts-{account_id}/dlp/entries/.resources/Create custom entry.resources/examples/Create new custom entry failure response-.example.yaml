$kind: http-example
id: 06a07807-f1b7-5122-be01-f388949643f6
name: Create new custom entry failure response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/entries"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "name": "string",
        "pattern": {
          "regex": "string"
        },
        "enabled": true,
        "description": "string",
        "profile_id": "988d2587-7337-5275-d70a-fe936dc1f11e"
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
