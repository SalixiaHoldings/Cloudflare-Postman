$kind: http-example
id: 3b0f911e-7021-5a4b-9e04-c3f116055dfa
name: Data tag category template read was successful.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/data_tag_category_templates"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
