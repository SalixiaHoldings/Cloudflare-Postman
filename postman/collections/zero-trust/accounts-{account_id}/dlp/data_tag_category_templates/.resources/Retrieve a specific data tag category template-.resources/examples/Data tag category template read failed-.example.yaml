$kind: http-example
id: dfc9a079-0c26-53cd-934d-c9e91428dc42
name: Data tag category template read failed.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/data_tag_category_templates/{{template_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
