$kind: http-example
id: 5166f871-2696-5275-b880-60abdb5b5e8a
name: Dataset creation failed.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/datasets"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "name": "string",
        "case_sensitive": true,
        "description": "string",
        "encoding_version": 5475,
        "secret": "{{secret}}"
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
