$kind: http-example
id: 12a8d70b-5df5-502d-8145-9ecaab710e27
name: Datasets read successfully.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/datasets"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
