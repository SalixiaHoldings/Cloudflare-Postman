$kind: http-example
id: bb9317c4-2d15-54fb-945d-aa76aacc7425
name: Dataset read successfully.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/datasets/{{dataset_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
