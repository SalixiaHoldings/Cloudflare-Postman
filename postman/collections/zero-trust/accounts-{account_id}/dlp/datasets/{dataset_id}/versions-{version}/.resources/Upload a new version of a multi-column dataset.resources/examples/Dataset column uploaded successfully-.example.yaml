$kind: http-example
id: 610b64d3-f13a-52f2-8378-07496a8c586e
name: Dataset column uploaded successfully.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/datasets/{{dataset_id}}/versions/{{version}}/entries/{{entry_id}}"
  method: POST
  headers:
    Content-Type: application/octet-stream
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: file
    content: {}
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
