$kind: http-example
id: e9a0f379-056e-56ab-b86d-94d6b8c86a04
name: Dataset column uploaded successfully.
request:
  url: "{{base_url}}/accounts/{{account_id}}/dlp/datasets/{{dataset_id}}/versions/{{version}}/entries/{{entry_id}}"
  method: POST
  headers:
    Content-Type: application/octet-stream
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: file
    content: {}
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
