$kind: http-example
id: a87dbedf-da79-5dee-91ee-025cec615891
request:
  url: "{{base_url}}/accounts/{{account_id}}/ai-gateway/gateways/{{gateway_id}}/provider_configs/{{id}}"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "secret": "{{secret}}"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": true,
        "result": {
          "id": "string",
          "provider_slug": "string",
          "secret_preview": "{{secret_preview}}",
          "default_config": false,
          "gateway_id": "my-gateway",
          "modified_at": "1926-11-10T16:01:31.799Z",
          "alias": "string",
          "secret_id": "{{secret_id}}",
          "rate_limit": 4432.372325100005,
          "rate_limit_period": 60
        }
      }
order: 1000
