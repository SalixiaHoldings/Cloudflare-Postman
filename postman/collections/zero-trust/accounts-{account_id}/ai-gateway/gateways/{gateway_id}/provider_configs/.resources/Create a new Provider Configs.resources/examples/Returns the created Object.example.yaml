$kind: http-example
id: 4c3e7848-9212-5240-986e-5d2b0baf8676
request:
  url: "{{base_url}}/accounts/{{account_id}}/ai-gateway/gateways/{{gateway_id}}/provider_configs"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "provider_slug": "string",
        "default_config": false,
        "alias": "string",
        "rate_limit": 7942.725408356637,
        "rate_limit_period": 60,
        "secret": "{{secret}}",
        "secret_id": "{{secret_id}}"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": true,
        "result": {
          "id": "string",
          "provider_slug": "string",
          "secret_preview": "{{secret_preview}}",
          "default_config": false,
          "gateway_id": "my-gateway",
          "modified_at": "1926-11-10T16:01:31.799Z",
          "alias": "string",
          "secret_id": "{{secret_id}}",
          "rate_limit": 4432.372325100005,
          "rate_limit_period": 60
        }
      }
order: 1000
