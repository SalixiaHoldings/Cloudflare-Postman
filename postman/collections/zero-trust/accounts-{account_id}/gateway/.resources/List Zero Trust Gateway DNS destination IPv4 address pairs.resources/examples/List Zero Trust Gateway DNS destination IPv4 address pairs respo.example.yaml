$kind: http-example
id: 74da96ac-e103-50fd-894b-8e96dc0c6795
name: List Zero Trust Gateway DNS destination IPv4 address pairs response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/gateway/dns_destination_ips"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ]
      }
order: 1000
