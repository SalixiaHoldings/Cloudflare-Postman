$kind: http-example
id: 9f36acf7-57d4-5b90-bfa0-b03ed6581fad
name: Get Zero Trust SSH settings response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/gateway/audit_ssh_settings"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
