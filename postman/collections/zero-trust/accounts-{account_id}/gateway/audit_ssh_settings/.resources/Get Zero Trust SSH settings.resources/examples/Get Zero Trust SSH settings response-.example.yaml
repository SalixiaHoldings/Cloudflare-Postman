$kind: http-example
id: 8190a214-29ba-520d-b861-cfae9f4e5db6
name: Get Zero Trust SSH settings response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/gateway/audit_ssh_settings"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
