$kind: http-example
id: a82b2fd6-9e2d-505d-9aba-e2a195ba0235
name: Rotate Zero Trust SSH account seed response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/gateway/audit_ssh_settings/rotate_seed"
  method: POST
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
