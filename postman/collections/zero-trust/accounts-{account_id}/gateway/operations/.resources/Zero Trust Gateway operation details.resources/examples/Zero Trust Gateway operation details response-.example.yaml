$kind: http-example
id: da0125de-2e1d-50d2-80e9-d08c7b35ffbc
name: Zero Trust Gateway operation details response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/gateway/operations/{{operation_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
