$kind: http-example
id: c0e3aa25-8a19-54e3-8c5a-c02af7b5fc9d
name: Returns a list of PAC files response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/gateway/pacfiles"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ]
      }
order: 1000
