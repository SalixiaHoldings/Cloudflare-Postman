$kind: http-example
id: 781f1079-8c57-53ce-9d19-291129a91814
name: Logging settings retrieval response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/gateway/logging"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
