$kind: http-example
id: 0e0053df-017b-5dec-82ce-87194147626d
name: Activates Zero Trust certificate details response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/gateway/certificates/{{certificate_id}}/activate"
  method: POST
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 202
  statusText: Accepted
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
