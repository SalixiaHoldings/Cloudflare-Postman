$kind: http-example
id: fc3d0c38-4446-57f7-8388-3a74f3af3f29
name: Deactivate Zero Trust certificate details response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/gateway/certificates/{{certificate_id}}/deactivate"
  method: POST
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 201
  statusText: Created
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
