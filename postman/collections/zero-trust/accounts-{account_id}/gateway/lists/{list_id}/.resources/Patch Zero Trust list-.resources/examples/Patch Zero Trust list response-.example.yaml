$kind: http-example
id: aaebbea7-53b6-5f1f-9edc-792af6b5bf2d
name: Patch Zero Trust list response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/gateway/lists/{{list_id}}"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: "{}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
