$kind: http-example
id: 64328176-535b-545d-8763-f6e3ef3c3ccd
name: Create Zero Trust list from CSV response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/gateway/lists/upload"
  method: POST
  headers:
    Content-Type: multipart/form-data
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: formdata
    content:
      - type: file
        key: file
        src: ""
        description: (Required) The CSV file containing list items. Must be `text/csv` or `text/plain` and cannot exceed 2 MB.
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
