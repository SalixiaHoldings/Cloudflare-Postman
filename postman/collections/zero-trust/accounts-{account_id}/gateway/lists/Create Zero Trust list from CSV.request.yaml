$kind: http-request
id: 7d8d91f8-a96a-5821-bfdb-23acd4073345
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: zero-trust-lists-create-zero-trust-list-from-csv
  POST /accounts/{account_id}/gateway/lists/upload

  Create a new Zero Trust list by uploading a CSV file. The file must be `text/csv` or `text/plain` and cannot exceed 2 MB. The operation is processed asynchronously. Use the returned operation ID to poll for status.

  Authentication support: multi-scheme-or-other
  Auth declaration SHA-256: cb039fe92c74f6483aaad6a3be8d90dfc474320bb13b733f4b11d38e19a5507f
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[],"api_token":[]}]
  Selected requirement: {"api_email":[],"api_key":[],"api_token":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/gateway/lists/upload"
method: POST
headers:
  Content-Type: multipart/form-data
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
body:
  type: formdata
  content:
    - type: file
      key: file
      src: ""
      description: (Required) The CSV file containing list items. Must be `text/csv` or `text/plain` and cannot exceed 2 MB.
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/Create Zero Trust list from CSV.resources/examples
order: 3000
