$kind: http-example
id: 32cd82a2-0480-5fc4-996f-ed8ab46db87c
name: Get Global WARP disconnect state response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/devices/resilience/disconnect"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
