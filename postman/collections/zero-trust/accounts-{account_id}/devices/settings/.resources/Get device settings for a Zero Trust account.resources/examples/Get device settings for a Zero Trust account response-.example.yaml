$kind: http-example
id: 6ec9658e-8b54-515e-b3d5-f1e926a00403
name: Get device settings for a Zero Trust account response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/devices/settings"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
