$kind: http-example
id: b2d41a79-c993-55e8-acd6-86fb2555b6ba
name: Reset response for device settings for a Zero Trust account.
request:
  url: "{{base_url}}/accounts/{{account_id}}/devices/settings"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
