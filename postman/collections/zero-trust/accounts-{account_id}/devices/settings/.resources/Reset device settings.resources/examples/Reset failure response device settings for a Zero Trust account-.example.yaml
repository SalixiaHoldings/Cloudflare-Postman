$kind: http-example
id: 4350f690-eca2-5004-93e0-f9b5d13264c6
name: Reset failure response device settings for a Zero Trust account.
request:
  url: "{{base_url}}/accounts/{{account_id}}/devices/settings"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
