$kind: http-example
id: 85f845a4-255e-5ba7-aabd-3b7c9ab78f88
name: Get device details response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/devices/{{device_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
