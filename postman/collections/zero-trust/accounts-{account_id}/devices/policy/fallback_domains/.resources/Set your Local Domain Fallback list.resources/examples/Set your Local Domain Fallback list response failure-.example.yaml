$kind: http-example
id: 4276cc45-cf1e-5a49-b039-f50aecba3fe7
name: Set your Local Domain Fallback list response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/devices/policy/fallback_domains"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      [
        {
          "suffix": "example.com"
        },
        {
          "suffix": "example.com"
        }
      ]
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
