$kind: http-example
id: b6bc1fec-f07c-57a1-a717-766406bdeedd
name: Set the Split Tunnel include list response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/devices/policy/include"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      [
        {
          "address": "192.0.2.0/24"
        },
        {
          "address": "192.0.2.0/24"
        }
      ]
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
