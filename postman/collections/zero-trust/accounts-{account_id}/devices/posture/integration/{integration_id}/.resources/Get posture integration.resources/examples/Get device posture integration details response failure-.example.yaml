$kind: http-example
id: 127a3951-4823-517d-96c3-3647b3d3a9e1
name: Get device posture integration details response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/devices/posture/integration/{{integration_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
