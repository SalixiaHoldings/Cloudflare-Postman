$kind: http-example
id: 555142ca-e592-5dcf-94cc-cfa6fa619dfe
name: Delete a device posture rule response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/devices/posture/{{rule_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
