$kind: http-example
id: 2e0ca08c-c0ce-5608-bc83-e91889ad23a3
name: Get device managed network details response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/devices/networks/{{network_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
