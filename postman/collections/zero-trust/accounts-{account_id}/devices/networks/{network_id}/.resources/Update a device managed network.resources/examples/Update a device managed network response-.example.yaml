$kind: http-example
id: d5a87b4c-45c9-5693-9289-6067e6d308c5
name: Update a device managed network response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/devices/networks/{{network_id}}"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "config": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        },
        "name": "managed-network-1",
        "type": "tls"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
