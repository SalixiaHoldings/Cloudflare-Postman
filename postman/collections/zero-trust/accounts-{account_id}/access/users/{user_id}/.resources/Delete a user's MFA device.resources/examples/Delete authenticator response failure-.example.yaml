$kind: http-example
id: 7938b75c-6f79-5965-808a-7feee370152a
name: Delete authenticator response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/access/users/{{user_id}}/mfa_authenticators/{{authenticator_id}}"
  method: DELETE
  headers:
    Accept: application/json
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
