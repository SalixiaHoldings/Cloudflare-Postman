$kind: http-request
id: 0022e78a-a421-5b41-a7c8-076ca9930932
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: ip-access-rules-for-a-user-list-ip-access-rules
  GET /user/firewall/access_rules/rules

  Fetches IP Access rules of the user. You can filter the results using several optional parameters.

  Authentication support: multi-scheme-or-other
  Auth declaration SHA-256: cb039fe92c74f6483aaad6a3be8d90dfc474320bb13b733f4b11d38e19a5507f
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[],"api_token":[]}]
  Selected requirement: {"api_email":[],"api_key":[],"api_token":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/user/firewall/access_rules/rules?mode=challenge&configuration.target=ip&configuration.value=198.51.100.4&notes=my note&match=all&page=1&per_page=20&order=mode&direction=desc"
method: GET
headers:
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
queryParams:
  mode: challenge
  configuration.target: ip
  configuration.value: 198.51.100.4
  notes: my note
  match: all
  page: "1"
  per_page: "20"
  order: mode
  direction: desc
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/List IP Access rules.resources/examples
order: 1000
