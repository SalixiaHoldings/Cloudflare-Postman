$kind: http-example
id: 7421c596-ec07-5788-af4d-c52ed30832f0
name: Delete an IP Access rule response.
request:
  url: "{{base_url}}/user/firewall/access_rules/rules/{{rule_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "id": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        }
      }
order: 1000
