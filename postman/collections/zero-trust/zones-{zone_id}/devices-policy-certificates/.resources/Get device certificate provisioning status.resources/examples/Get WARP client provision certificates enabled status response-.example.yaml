$kind: http-example
id: 2b7d7333-3559-596c-ae25-a7f7bbfc57dc
name: Get WARP client provision certificates enabled status response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/devices/policy/certificates"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
