$kind: http-example
id: bdcf3f2c-0636-5b9c-8270-df9f6b7a15d9
request:
  url: "{{base_url}}/accounts/{{account_id}}/mnm/config/full"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
