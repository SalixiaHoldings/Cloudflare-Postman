$kind: http-example
id: 5ea73053-3629-57e5-b0fb-9a39e639f040
request:
  url: "{{base_url}}/accounts/{{account_id}}/mnm/config/full"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
