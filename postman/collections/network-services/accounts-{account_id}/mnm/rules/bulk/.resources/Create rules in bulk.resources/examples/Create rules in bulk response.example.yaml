$kind: http-example
id: a83ee6a9-e520-5fa1-987d-5824534e188d
request:
  url: "{{base_url}}/accounts/{{account_id}}/mnm/rules/bulk"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      [
        {
          "name": "my_rule_1",
          "prefixes": [],
          "automatic_advertisement": false,
          "type": "zscore"
        }
      ]
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ]
      }
order: 1000
