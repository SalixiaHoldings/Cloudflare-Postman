$kind: http-example
id: b402666e-841a-5af1-97c2-4fb11bf845e0
name: Monitor Details response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/load_balancers/monitors/{{monitor_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
