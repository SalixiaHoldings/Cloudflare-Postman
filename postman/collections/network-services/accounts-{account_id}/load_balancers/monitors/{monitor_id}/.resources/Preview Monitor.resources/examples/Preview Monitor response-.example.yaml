$kind: http-example
id: 53612dac-d7ec-5ea9-9d7a-a812c274474e
name: Preview Monitor response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/load_balancers/monitors/{{monitor_id}}/preview"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: "{}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "pools": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          "preview_id": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        }
      }
order: 1000
