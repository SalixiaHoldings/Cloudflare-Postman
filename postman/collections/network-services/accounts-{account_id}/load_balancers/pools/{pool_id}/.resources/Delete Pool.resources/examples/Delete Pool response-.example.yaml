$kind: http-example
id: abecd61a-4be2-5805-876b-ce181d9c6d09
name: Delete Pool response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/load_balancers/pools/{{pool_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "id": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        }
      }
order: 1000
