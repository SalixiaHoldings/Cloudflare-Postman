$kind: http-request
id: 1e47f67c-e0c9-511d-adeb-54b4e53100ae
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: account-load-balancer-monitor-groups-patch-monitor-group
  PATCH /accounts/{account_id}/load_balancers/monitor_groups/{monitor_group_id}

  Apply changes to an existing monitor group, overwriting the supplied properties.

  Authentication support: multi-scheme-or-other
  Auth declaration SHA-256: cb039fe92c74f6483aaad6a3be8d90dfc474320bb13b733f4b11d38e19a5507f
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[],"api_token":[]}]
  Selected requirement: {"api_email":[],"api_key":[],"api_token":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/load_balancers/monitor_groups/{{monitor_group_id}}"
method: PATCH
headers:
  Content-Type: application/json
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
body:
  type: json
  content: |-
    {
      "description": "Primary datacenter monitors",
      "members": [
        {
          "monitor_id": "{{members__0__monitor_id}}",
          "enabled": true,
          "monitoring_only": false,
          "must_be_healthy": true
        },
        {
          "monitor_id": "{{members__1__monitor_id}}",
          "enabled": true,
          "monitoring_only": false,
          "must_be_healthy": true
        }
      ]
    }
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/Patch Monitor Group.resources/examples
order: 3000
