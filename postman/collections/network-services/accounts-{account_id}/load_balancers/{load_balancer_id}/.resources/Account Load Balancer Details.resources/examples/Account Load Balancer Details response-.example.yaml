$kind: http-example
id: 4e5e2381-c06c-5e95-9cce-5a50c09dcc62
name: Account Load Balancer Details response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/load_balancers/{{load_balancer_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
