$kind: http-example
id: 402203ca-d026-5294-909c-d6c90ad7e342
name: Delete Account Load Balancer response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/load_balancers/{{load_balancer_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "id": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        }
      }
order: 1000
