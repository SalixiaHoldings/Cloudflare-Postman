$kind: http-example
id: 7b73f235-22b5-58ef-a152-1ea26a23c473
name: Preview Result response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/load_balancers/preview/{{preview_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
