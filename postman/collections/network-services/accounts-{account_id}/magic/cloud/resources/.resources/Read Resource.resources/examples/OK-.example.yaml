$kind: http-example
id: c14b657c-2ef8-5608-9360-16beafb7fb0f
name: OK.
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/cloud/resources/{{resource_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: v2
      value: "true"
      disabled: true
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
