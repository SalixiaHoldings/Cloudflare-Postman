$kind: http-example
id: 9fba95fe-d7bf-5fe0-b4d3-f74d0858636f
name: Conflict.
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/cloud/providers/{{provider_id}}"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "aws_arn": "string",
        "azure_subscription_id": "string",
        "azure_tenant_id": "string",
        "description": "string",
        "friendly_name": "string",
        "gcp_project_id": "string",
        "gcp_service_account_email": "string"
      }
response:
  statusCode: 409
  statusText: Conflict
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": null,
        "success": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        },
        "errors": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ],
        "messages": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 6000
