$kind: http-example
id: d00eda18-042a-5092-a2a3-b9be3bbf02f5
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/routes/{{route_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "deleted": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          "deleted_route": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        }
      }
order: 1000
