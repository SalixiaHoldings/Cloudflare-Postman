$kind: http-example
id: 93acbe08-9e49-5c4f-94e6-94e028770d55
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/routes"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "prefix": "192.0.2.0/24",
        "nexthop": "203.0.113.1",
        "priority": 3031,
        "description": "New route for new prefix 203.0.113.1",
        "weight": 304
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
