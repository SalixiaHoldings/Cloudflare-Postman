$kind: http-example
id: 9dcde36f-ece1-59b9-9365-f7c52e49c3b8
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/routes"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "routes": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        }
      }
order: 1000
