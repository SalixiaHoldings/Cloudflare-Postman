$kind: http-example
id: 3a43f9ed-c544-5a9b-9240-7fd5925bc83d
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/cf1_sites/{{cf1_site_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
