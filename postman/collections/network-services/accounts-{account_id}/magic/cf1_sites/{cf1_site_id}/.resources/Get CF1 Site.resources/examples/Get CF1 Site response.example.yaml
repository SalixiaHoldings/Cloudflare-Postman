$kind: http-example
id: 383e25af-aacb-5894-9c55-40cd17ec3db5
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/cf1_sites/{{cf1_site_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
