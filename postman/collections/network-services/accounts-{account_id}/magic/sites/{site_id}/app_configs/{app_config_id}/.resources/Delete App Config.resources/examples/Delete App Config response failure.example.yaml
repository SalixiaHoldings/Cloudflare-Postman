$kind: http-example
id: 5a596504-909b-5317-88a7-762b0bcf58ad
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/sites/{{site_id}}/app_configs/{{app_config_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
