$kind: http-example
id: 6aa712cf-8cc5-5b1c-8e68-8f48989646f8
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/sites/{{site_id}}/app_configs/{{app_config_id}}"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "account_app_id": "ac60d3d0435248289d446cedd870bcf4",
        "breakout": true,
        "managed_app_id": "cloudflare",
        "priority": 0
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
