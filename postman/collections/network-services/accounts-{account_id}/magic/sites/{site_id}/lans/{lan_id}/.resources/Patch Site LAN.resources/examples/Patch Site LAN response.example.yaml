$kind: http-example
id: 68cfb52b-9b51-5458-9da7-55911ab92ee4
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/sites/{{site_id}}/lans/{{lan_id}}"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "bond_id": 2,
        "is_breakout": false,
        "is_prioritized": true,
        "name": "string",
        "physport": 1,
        "static_addressing": {
          "address": "192.0.2.0/24"
        },
        "vlan_tag": 42
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
