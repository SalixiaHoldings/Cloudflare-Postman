$kind: http-example
id: 0cbc7f32-c88f-560c-b3f0-9f2bf856f4f8
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/sites/{{site_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
