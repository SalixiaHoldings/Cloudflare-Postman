$kind: http-example
id: ffc8cf4a-cd27-5e0c-a099-4927a64660d4
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/sites/{{site_id}}/netflow_config"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "active_timeout": 30,
        "collector_ip": "162.159.65.1",
        "collector_port": 2055,
        "inactive_timeout": 15,
        "sampling_rate": 100
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
