$kind: http-example
id: ab9e63b4-ba5c-5916-86d6-94308fc5cac2
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/sites/{{site_id}}/netflow_config"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
