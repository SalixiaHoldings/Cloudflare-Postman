$kind: http-request
id: 8dcc359c-4630-50cd-9f13-64d2b3aaf420
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: magic-site-netflow-config-create-netflow-config
  POST /accounts/{account_id}/magic/sites/{site_id}/netflow_config

  Creates a NetFlow configuration for a site.

  Authentication support: multi-scheme-or-other
  Auth declaration SHA-256: cb039fe92c74f6483aaad6a3be8d90dfc474320bb13b733f4b11d38e19a5507f
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[],"api_token":[]}]
  Selected requirement: {"api_email":[],"api_key":[],"api_token":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/magic/sites/{{site_id}}/netflow_config"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
body:
  type: json
  content: |-
    {
      "active_timeout": 30,
      "collector_ip": "162.159.65.1",
      "collector_port": 2055,
      "inactive_timeout": 15,
      "sampling_rate": 100
    }
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/Create NetFlow Configuration.resources/examples
order: 4000
