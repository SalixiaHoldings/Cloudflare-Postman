$kind: http-example
id: e2c97c1e-26f6-5983-92fa-2394898930c8
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/sites/{{site_id}}/netflow_config"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "active_timeout": 30,
        "collector_ip": "162.159.65.1",
        "collector_port": 2055,
        "inactive_timeout": 15,
        "sampling_rate": 100
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
