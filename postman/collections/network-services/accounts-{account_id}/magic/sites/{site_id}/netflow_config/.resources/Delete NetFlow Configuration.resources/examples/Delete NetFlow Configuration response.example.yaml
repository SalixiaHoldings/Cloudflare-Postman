$kind: http-example
id: 65e88f44-bff5-567b-b3ce-8739520b6bec
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/sites/{{site_id}}/netflow_config"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
