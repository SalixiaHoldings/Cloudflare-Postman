$kind: http-example
id: fcad8410-75da-5be7-9d8d-f7cc393be013
request:
  url: "{{base_url}}/accounts/{{account_id}}/magic/connectors"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: device_type
      value: MANAGED
      disabled: true
      description: Filter connectors by device type.
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ]
      }
order: 1000
