$kind: http-example
id: da428e04-0d05-5c99-96c2-035245db768b
name: Delete Monitor response.
request:
  url: "{{base_url}}/user/load_balancers/monitors/{{monitor_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "id": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        }
      }
order: 1000
