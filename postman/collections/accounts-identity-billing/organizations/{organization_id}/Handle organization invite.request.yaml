$kind: http-request
id: 0a4fdbe3-ba74-54b8-8ffb-605da6648e0a
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: Organizations_handleOrganizationInvite
  PUT /organizations/{organization_id}/invites/{member_code}

  Accept or reject an invitation to a specific organization. (Currently in Public Beta - see https://developers.cloudflare.com/fundamentals/organizations/)

  Authentication support: legacy-only
  Auth declaration SHA-256: a9720528d5568ee0454de8ee346c99dd84fa50a430a5033cc320160dae1660d9
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[]}]
  Selected requirement: {"api_email":[],"api_key":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/organizations/{{organization_id}}/invites/{{member_code}}"
method: PUT
headers:
  Content-Type: application/json
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
body:
  type: json
  content: |-
    {
      "status": "reject"
    }
auth:
  type: noauth
examples: ./.resources/Handle organization invite.resources/examples
order: 6000
