$kind: http-request
id: 65d08d61-6cfc-5488-aa1c-6bf8736c0c42
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: Members_batchCreate
  POST /organizations/{organization_id}/members:batchCreate

  Batch create multiple memberships that grant access to a specific Organization.

  Authentication support: legacy-only
  Auth declaration SHA-256: a9720528d5568ee0454de8ee346c99dd84fa50a430a5033cc320160dae1660d9
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[]}]
  Selected requirement: {"api_email":[],"api_key":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/organizations/{{organization_id}}/members:batchCreate"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
body:
  type: json
  content: |-
    {
      "members": [
        {
          "user": {
            "email": "{{members__0__user__email}}"
          }
        },
        {
          "user": {
            "email": "{{members__1__user__email}}"
          }
        }
      ]
    }
auth:
  type: noauth
examples: ./.resources/Batch create organization members.resources/examples
order: 7000
