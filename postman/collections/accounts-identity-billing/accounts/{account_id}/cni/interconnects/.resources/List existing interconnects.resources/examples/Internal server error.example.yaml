$kind: http-example
id: fa5aa584-4a72-50fc-9325-e4216793b49c
request:
  url: "{{base_url}}/accounts/{{account_id}}/cni/interconnects"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: site
      value: string
      disabled: true
      description: If specified, only show interconnects located at the given site
    - key: type
      value: string
      disabled: true
      description: If specified, only show interconnects of the given type
    - key: cursor
      value: "4690"
      disabled: true
    - key: limit
      value: "5193"
      disabled: true
response:
  statusCode: 500
  statusText: Internal Server Error
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: ""
order: 3000
