$kind: http-example
id: 47247022-e07f-5731-8b1c-90d913bfeaa5
request:
  url: "{{base_url}}/accounts/{{account_id}}/cni/interconnects"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "type": "{{type}}",
        "account": "{{account}}",
        "slot_id": "{{slot_id}}"
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: ""
order: 2000
