$kind: http-example
id: 706a3604-a01b-5697-8549-e29bca1b4321
request:
  url: "{{base_url}}/accounts/{{account_id}}/cni/interconnects/{{icon}}/loa"
  method: GET
  headers:
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: name
      value: string
      disabled: true
      description: Custom name to use in the LOA instead of the account name (200 Character limit)
response:
  statusCode: 404
  statusText: Not Found
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: ""
order: 3000
