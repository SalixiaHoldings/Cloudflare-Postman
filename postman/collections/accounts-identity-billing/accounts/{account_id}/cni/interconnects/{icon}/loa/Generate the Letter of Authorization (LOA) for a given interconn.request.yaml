$kind: http-request
id: 1f18a0f2-fc55-55d6-b77c-48905c13411d
name: Generate the Letter of Authorization (LOA) for a given interconnect
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: get_interconnect_loa
  GET /accounts/{account_id}/cni/interconnects/{icon}/loa

  Downloads the Letter of Authorization (LOA) for a network interconnect, required for
  physical cross-connect provisioning.

  Authentication support: multi-scheme-or-other
  Auth declaration SHA-256: cb039fe92c74f6483aaad6a3be8d90dfc474320bb13b733f4b11d38e19a5507f
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[],"api_token":[]}]
  Selected requirement: {"api_email":[],"api_key":[],"api_token":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/cni/interconnects/{{icon}}/loa"
method: GET
headers:
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
queryParams:
  - key: name
    value: string
    disabled: true
    description: Custom name to use in the LOA instead of the account name (200 Character limit)
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/Generate the Letter of Authorization (LOA) for a given interconn.resources/examples
order: 1000
