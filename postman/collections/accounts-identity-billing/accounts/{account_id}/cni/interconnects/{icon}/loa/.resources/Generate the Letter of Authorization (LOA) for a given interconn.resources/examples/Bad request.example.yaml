$kind: http-example
id: 355c42f4-c2de-5eb0-a307-658cedeed4c3
request:
  url: "{{base_url}}/accounts/{{account_id}}/cni/interconnects/{{icon}}/loa"
  method: GET
  headers:
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: name
      value: string
      disabled: true
      description: Custom name to use in the LOA instead of the account name (200 Character limit)
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: ""
order: 2000
