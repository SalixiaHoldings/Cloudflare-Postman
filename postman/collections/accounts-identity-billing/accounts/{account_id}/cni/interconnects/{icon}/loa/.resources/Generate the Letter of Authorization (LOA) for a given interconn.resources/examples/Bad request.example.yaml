$kind: http-example
id: 37ad4898-528e-50ba-8936-5a139b11e06c
request:
  url: "{{base_url}}/accounts/{{account_id}}/cni/interconnects/{{icon}}/loa"
  method: GET
  headers:
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: name
      value: string
      disabled: true
      description: Custom name to use in the LOA instead of the account name (200 Character limit)
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: ""
order: 2000
