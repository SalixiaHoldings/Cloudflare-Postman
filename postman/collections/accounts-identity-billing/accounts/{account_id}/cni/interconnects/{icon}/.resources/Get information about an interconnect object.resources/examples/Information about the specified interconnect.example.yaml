$kind: http-example
id: d66af539-1dc1-59b7-96ec-d31300db4eb6
request:
  url: "{{base_url}}/accounts/{{account_id}}/cni/interconnects/{{icon}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "value": "<Error: Too many levels of nesting to fake this schema>"
      }
order: 1000
