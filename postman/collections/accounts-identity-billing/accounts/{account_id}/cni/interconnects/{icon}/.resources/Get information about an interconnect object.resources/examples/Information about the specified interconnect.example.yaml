$kind: http-example
id: 3ab8873b-9d06-5cba-84d0-834cee8bc316
request:
  url: "{{base_url}}/accounts/{{account_id}}/cni/interconnects/{{icon}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "value": "<Error: Too many levels of nesting to fake this schema>"
      }
order: 1000
