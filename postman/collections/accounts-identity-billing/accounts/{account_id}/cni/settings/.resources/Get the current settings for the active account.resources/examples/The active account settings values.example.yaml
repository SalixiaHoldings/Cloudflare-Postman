$kind: http-example
id: db484283-740f-53b6-be95-87adbff3a070
request:
  url: "{{base_url}}/accounts/{{account_id}}/cni/settings"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "default_asn": 9228
      }
order: 1000
