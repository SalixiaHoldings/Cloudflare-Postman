$kind: http-example
id: e5973c88-9f86-586f-9cd1-9f42cc020d98
request:
  url: "{{base_url}}/accounts/{{account_id}}/cni/cnis"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: slot
      value: string
      disabled: true
      description: If specified, only show CNIs associated with the specified slot
    - key: tunnel_id
      value: string
      disabled: true
      description: If specified, only show cnis associated with the specified tunnel id
    - key: cursor
      value: "4690"
      disabled: true
    - key: limit
      value: "5193"
      disabled: true
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: ""
order: 2000
