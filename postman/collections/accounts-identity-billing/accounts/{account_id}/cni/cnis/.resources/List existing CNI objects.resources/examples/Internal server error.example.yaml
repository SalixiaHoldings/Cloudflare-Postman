$kind: http-example
id: 2add22b8-8b2f-5a73-812a-9cb4c8000bde
request:
  url: "{{base_url}}/accounts/{{account_id}}/cni/cnis"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: slot
      value: string
      disabled: true
      description: If specified, only show CNIs associated with the specified slot
    - key: tunnel_id
      value: string
      disabled: true
      description: If specified, only show cnis associated with the specified tunnel id
    - key: cursor
      value: "535"
      disabled: true
    - key: limit
      value: "629"
      disabled: true
response:
  statusCode: 500
  statusText: Internal Server Error
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: ""
order: 3000
