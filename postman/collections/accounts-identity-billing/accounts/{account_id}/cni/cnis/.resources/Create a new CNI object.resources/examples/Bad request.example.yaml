$kind: http-example
id: 9527013a-9f74-5905-acea-652ab7f3966d
request:
  url: "{{base_url}}/accounts/{{account_id}}/cni/cnis"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "interconnect": "string",
        "account": "<string>",
        "magic": {
          "conduit_name": "{{magic__conduit_name}}",
          "description": "{{magic__description}}",
          "mtu": 0
        },
        "bgp": {
          "customer_asn": 0,
          "extra_prefixes": []
        }
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: ""
order: 2000
