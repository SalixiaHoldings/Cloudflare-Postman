$kind: http-example
id: 980e3778-5652-56d7-9ed1-d7049bc4b65a
request:
  url: "{{base_url}}/accounts/{{account_id}}/cni/cnis"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: slot
      value: string
      disabled: true
      description: If specified, only show CNIs associated with the specified slot
    - key: tunnel_id
      value: string
      disabled: true
      description: If specified, only show cnis associated with the specified tunnel id
    - key: cursor
      value: "535"
      disabled: true
    - key: limit
      value: "629"
      disabled: true
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "items": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ],
        "next": 5764
      }
order: 1000
