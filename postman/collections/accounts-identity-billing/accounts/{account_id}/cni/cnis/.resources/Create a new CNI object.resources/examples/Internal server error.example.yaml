$kind: http-example
id: ecd2ca7e-2de9-590e-a5a8-b591a777c674
request:
  url: "{{base_url}}/accounts/{{account_id}}/cni/cnis"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "interconnect": "string",
        "account": "<string>",
        "magic": {
          "conduit_name": "{{magic__conduit_name}}",
          "description": "{{magic__description}}",
          "mtu": 0
        },
        "bgp": {
          "customer_asn": 0,
          "extra_prefixes": []
        }
      }
response:
  statusCode: 500
  statusText: Internal Server Error
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: ""
order: 4000
