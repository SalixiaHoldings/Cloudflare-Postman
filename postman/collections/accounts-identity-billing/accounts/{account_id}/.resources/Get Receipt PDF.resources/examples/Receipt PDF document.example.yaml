$kind: http-example
id: e2762387-9f64-50c1-b692-dd77bd44eceb
request:
  url: "{{base_url}}/accounts/{{account_id}}/receipts/{{receipt_id}}/pdf"
  method: GET
  headers:
    Accept: application/pdf
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: doctype
      value: string
      disabled: true
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/pdf
  body:
    type: text
    content: string
order: 1000
