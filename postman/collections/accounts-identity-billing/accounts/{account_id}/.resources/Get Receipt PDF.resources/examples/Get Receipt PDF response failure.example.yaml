$kind: http-example
id: 066efc68-a0af-5dc5-8fee-d71d862f0413
request:
  url: "{{base_url}}/accounts/{{account_id}}/receipts/{{receipt_id}}/pdf"
  method: GET
  headers:
    Accept: application/pdf
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: doctype
      value: string
      disabled: true
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
