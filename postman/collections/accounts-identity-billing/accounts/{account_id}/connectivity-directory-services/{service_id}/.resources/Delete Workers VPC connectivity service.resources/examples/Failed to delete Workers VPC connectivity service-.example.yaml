$kind: http-example
id: fd067f0d-34a0-5a65-8fda-903729f61969
name: Failed to delete Workers VPC connectivity service.
request:
  url: "{{base_url}}/accounts/{{account_id}}/connectivity/directory/services/{{service_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
