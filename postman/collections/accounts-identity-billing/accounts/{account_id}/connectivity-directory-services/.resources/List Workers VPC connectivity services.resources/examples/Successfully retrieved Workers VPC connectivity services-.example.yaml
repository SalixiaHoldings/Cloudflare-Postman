$kind: http-example
id: 9f65ddc9-bcff-53e8-84f6-934ad31fc9cc
name: Successfully retrieved Workers VPC connectivity services.
request:
  url: "{{base_url}}/accounts/{{account_id}}/connectivity/directory/services"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: type
      value: tcp
      disabled: true
    - key: page
      value: "1"
      disabled: true
      description: Current page in the response
    - key: per_page
      value: "1000"
      disabled: true
      description: Max amount of entries returned per page
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
