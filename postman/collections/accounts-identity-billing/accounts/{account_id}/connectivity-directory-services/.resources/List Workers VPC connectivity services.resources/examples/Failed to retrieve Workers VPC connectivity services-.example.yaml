$kind: http-example
id: fa32f786-e489-535b-aca8-dfa74e89c83f
name: Failed to retrieve Workers VPC connectivity services.
request:
  url: "{{base_url}}/accounts/{{account_id}}/connectivity/directory/services"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: type
      value: http
      disabled: true
    - key: page
      value: "1"
      disabled: true
      description: Current page in the response
    - key: per_page
      value: "1000"
      disabled: true
      description: Max amount of entries returned per page
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
