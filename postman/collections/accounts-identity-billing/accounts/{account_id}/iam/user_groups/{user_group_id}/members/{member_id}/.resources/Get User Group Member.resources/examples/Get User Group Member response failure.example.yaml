$kind: http-example
id: fd348f8e-b994-54a9-8359-3f29fe1fd2a2
request:
  url: "{{base_url}}/accounts/{{account_id}}/iam/user_groups/{{user_group_id}}/members/{{member_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
