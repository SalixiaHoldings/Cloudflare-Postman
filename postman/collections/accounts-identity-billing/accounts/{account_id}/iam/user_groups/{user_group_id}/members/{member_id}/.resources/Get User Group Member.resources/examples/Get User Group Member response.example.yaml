$kind: http-example
id: 50aa8bab-02d5-5a74-aa71-b630b99cedbd
request:
  url: "{{base_url}}/accounts/{{account_id}}/iam/user_groups/{{user_group_id}}/members/{{member_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
