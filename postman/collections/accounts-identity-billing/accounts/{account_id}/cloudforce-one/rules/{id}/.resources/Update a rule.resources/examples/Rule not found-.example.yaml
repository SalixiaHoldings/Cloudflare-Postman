$kind: http-example
id: 749d30e3-5256-5e02-85ff-cc12a292e195
name: Rule not found.
request:
  url: "{{base_url}}/accounts/{{account_id}}/cloudforce-one/rules/{{id}}"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "commit_message": "Reduce false positives on legit CI workers.",
        "content": "rule example { condition: true }",
        "description": "Detects malicious proxy workers",
        "enabled": true,
        "is_public": false,
        "name": "block-malicious-workers",
        "namespaces": [
          "yara/workers"
        ],
        "path": "yara/workers"
      }
response:
  statusCode: 404
  statusText: Not Found
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "error": "Not found"
      }
order: 4000
