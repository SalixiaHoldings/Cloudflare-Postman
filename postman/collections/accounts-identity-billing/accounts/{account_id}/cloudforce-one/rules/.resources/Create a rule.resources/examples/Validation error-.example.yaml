$kind: http-example
id: 88c67d7a-9812-5343-aacf-db930b17979a
name: Validation error.
request:
  url: "{{base_url}}/accounts/{{account_id}}/cloudforce-one/rules"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "name": "block-malicious-workers",
        "path": "yara/workers",
        "content": "rule example { condition: true }",
        "commit_message": "Add worker rule to catch bpb-panel variants.",
        "description": "Detects malicious proxy workers",
        "enabled": true,
        "is_public": false
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "error": "Not found"
      }
order: 3000
