$kind: http-example
id: 2aa86b02-e44c-552a-a27b-cc87dd612974
name: Returns a list of attackers.
request:
  url: "{{base_url}}/accounts/{{account_id}}/cloudforce-one/events/attackers"
  method: GET
  headers:
    Accept: application/json
  queryParams:
    - key: datasetIds
      value: string
      disabled: true
      description: Array of dataset IDs to query attackers from. If not provided, uses the default dataset.
    - key: datasetIds
      value: string
      disabled: true
      description: Array of dataset IDs to query attackers from. If not provided, uses the default dataset.
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
order: 1000
