$kind: http-example
id: c9554cce-c870-5eff-858d-0428fa0d3ea4
name: Bad Request.
request:
  url: "{{base_url}}/accounts/{{account_id}}/cloudforce-one/events/categories?datasetIds=string&datasetIds=string"
  method: GET
  headers:
    Accept: application/json
  queryParams:
    - key: datasetIds
      value: string
      description: Array of dataset IDs to query categories from. If not provided, uses the default dataset.
    - key: datasetIds
      value: string
      description: Array of dataset IDs to query categories from. If not provided, uses the default dataset.
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {},
        "success": true,
        "errors": [
          {
            "message": {
              "value": "<Error: Too many levels of nesting to fake this schema>"
            }
          },
          {
            "message": {
              "value": "<Error: Too many levels of nesting to fake this schema>"
            }
          }
        ]
      }
order: 2000
