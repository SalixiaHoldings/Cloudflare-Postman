$kind: http-request
id: d0ed4124-2587-548d-abf2-d5ed168d351d
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: createApplication
  POST /accounts/{account_id}/containers/applications

  Create a Containers application.

  Use `scheduling_policy: "default"` for a scheduler-backed application. The
  Containers scheduler maintains the requested instance count and manages
  deployment configuration, placement, scaling, versions, and rollouts.

  Use `scheduling_policy: "durable_object"` for a Durable Object-managed
  application. Each Durable Object creates and manages the lifecycle of its
  container instance. Supply `name`, `scheduling_policy`, and `durable_objects`,
  with optional `configuration` and optional top-level `observability` settings.
  Deployment configuration, scaling, constraints, versions, and rollouts do not apply.


  Authentication support: bearer-alternative
  Auth declaration SHA-256: 4844622a422ead8f8ff95dad7881ac91bb3b64cd78aae19a345e1aaa5456fdd3
  Upstream security (operation; OR alternatives, AND within each object): [{"api_token":[]},{"api_email":[],"api_key":[]}]
  Selected requirement: {"api_token":[]}
  Use the narrowly scoped api_token; legacy alternatives are not enabled.
url: "{{base_url}}/accounts/{{account_id}}/containers/applications"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "name": "{{name}}",
      "scheduling_policy": "default",
      "instances": 0,
      "max_instances": 0,
      "configuration": {
        "image": "{{configuration__image}}"
      }
    }
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/Create a new application.resources/examples
order: 2000
