$kind: http-request
id: 3f571988-6b32-522b-bee5-05a457198d60
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: pay-per-crawl.getTermsSignature
  GET /accounts/{account_id}/pay-per-crawl/terms/signature

  Gets the account's pay-per-crawl terms signature status.

  Authentication support: legacy-only
  Auth declaration SHA-256: a9720528d5568ee0454de8ee346c99dd84fa50a430a5033cc320160dae1660d9
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[]}]
  Selected requirement: {"api_email":[],"api_key":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/pay-per-crawl/terms/signature"
method: GET
headers:
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
queryParams:
  - key: remote
    value: "true"
    disabled: true
    description: Refresh the signature status from Ironclad.
auth:
  type: noauth
examples: ./.resources/Get pay-per-crawl terms signature status.resources/examples
order: 1000
