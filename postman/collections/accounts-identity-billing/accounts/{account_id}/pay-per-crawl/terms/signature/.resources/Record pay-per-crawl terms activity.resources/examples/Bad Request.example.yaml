$kind: http-example
id: c8c0f09a-d5f7-5b20-ac3c-b40aaa9881e6
request:
  url: "{{base_url}}/accounts/{{account_id}}/pay-per-crawl/terms/signature?event_type=displayed&vid=string"
  method: POST
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: event_type
      value: displayed
      description: (Required) The terms activity to record.
    - key: vid
      value: string
      description: (Required) Ironclad contract version ID.
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ],
        "result": "",
        "success": true
      }
order: 2000
