$kind: http-example
id: 03e879e8-2d13-5ce0-aec4-bf293a5f79f8
request:
  url: "{{base_url}}/accounts/{{account_id}}/pay-per-crawl/terms/signature?event_type=agreed&vid=string"
  method: POST
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: event_type
      value: agreed
      description: (Required) The terms activity to record.
    - key: vid
      value: string
      description: (Required) Ironclad contract version ID.
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ],
        "result": "",
        "success": false
      }
order: 2000
