$kind: http-example
id: 61243de0-9313-571a-b592-29cd5980b82a
request:
  url: "{{base_url}}/accounts/{{account_id}}/pay-per-crawl/terms/signature"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: remote
      value: "true"
      disabled: true
      description: Refresh the signature status from Ironclad.
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ],
        "result": "",
        "success": false
      }
order: 2000
