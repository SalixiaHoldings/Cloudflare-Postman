$kind: http-example
id: 61c638fe-436e-5133-a1dc-cd15b3c09e02
request:
  url: "{{base_url}}/accounts/{{account_id}}/pay-per-crawl/terms/signature?remote=true"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: remote
      value: "true"
      description: Refresh the signature status from Ironclad.
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ],
        "result": "",
        "success": false
      }
order: 2000
