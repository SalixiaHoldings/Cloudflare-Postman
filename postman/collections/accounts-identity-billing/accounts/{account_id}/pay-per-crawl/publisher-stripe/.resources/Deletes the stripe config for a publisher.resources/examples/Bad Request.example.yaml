$kind: http-example
id: 035d87cf-6b7e-5aaa-bbc8-82d3d9f088c8
request:
  url: "{{base_url}}/accounts/{{account_id}}/pay-per-crawl/publisher/stripe"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ],
        "result": "",
        "success": true
      }
order: 2000
