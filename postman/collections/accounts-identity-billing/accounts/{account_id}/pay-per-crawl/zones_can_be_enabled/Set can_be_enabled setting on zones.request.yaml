$kind: http-request
id: 5a102125-c494-59ba-a5d9-81615918d798
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: pay-per-crawl.setZonesCanBeEnabled
  PATCH /accounts/{account_id}/pay-per-crawl/zones_can_be_enabled

  Allows an account admin to set the can_be_enabled setting on a list of zones. An omitted can_be_enabled value leaves that zone unchanged.

  Authentication support: legacy-only
  Auth declaration SHA-256: a9720528d5568ee0454de8ee346c99dd84fa50a430a5033cc320160dae1660d9
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[]}]
  Selected requirement: {"api_email":[],"api_key":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/pay-per-crawl/zones_can_be_enabled"
method: PATCH
headers:
  Content-Type: application/json
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
body:
  type: json
  content: |-
    {
      "zones": [
        {
          "id": "{{zones__0__id}}"
        }
      ]
    }
auth:
  type: noauth
examples: ./.resources/Set can_be_enabled setting on zones.resources/examples
order: 1000
