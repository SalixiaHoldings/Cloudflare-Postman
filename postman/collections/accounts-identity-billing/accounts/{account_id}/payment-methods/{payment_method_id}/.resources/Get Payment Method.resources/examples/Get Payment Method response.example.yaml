$kind: http-example
id: 1632bdc3-5c2e-533d-9fe2-bf98f6d91bb2
request:
  url: "{{base_url}}/accounts/{{account_id}}/payment-methods/{{payment_method_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
