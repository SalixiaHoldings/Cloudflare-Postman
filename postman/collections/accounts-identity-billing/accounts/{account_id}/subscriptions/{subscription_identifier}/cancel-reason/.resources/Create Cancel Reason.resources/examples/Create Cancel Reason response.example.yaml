$kind: http-example
id: ef4d6b96-3e46-51b2-9ee8-5f8c7002740a
request:
  url: "{{base_url}}/accounts/{{account_id}}/subscriptions/{{subscription_identifier}}/cancel-reason"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "other": "string",
        "reason_code": [
          "string",
          "string"
        ]
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
