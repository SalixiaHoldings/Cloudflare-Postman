$kind: http-example
id: 92b8bf0a-3ad4-5c1c-b0ed-85c55f212b92
request:
  url: "{{base_url}}/accounts/{{account_id}}/subscriptions/{{subscription_identifier}}/cancel-reason"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
