$kind: http-example
id: dd21e7d7-10c4-59ba-b72b-f98de6b749a2
request:
  url: "{{base_url}}/accounts/{{account_id}}/intel/indicator-feeds/permissions/createProvider"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "account_id": "{{account_id}}",
        "name": "my_provider"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
