$kind: http-example
id: 9c3e3f71-e6c8-5099-a17a-d211675f338d
request:
  url: "{{base_url}}/accounts/{{account_id}}/intel/indicator-feeds/{{feed_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
