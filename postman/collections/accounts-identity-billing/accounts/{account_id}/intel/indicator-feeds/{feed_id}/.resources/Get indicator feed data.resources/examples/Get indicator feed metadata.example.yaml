$kind: http-example
id: b6062552-bd3b-559a-ace6-efa652fd4e39
request:
  url: "{{base_url}}/accounts/{{account_id}}/intel/indicator-feeds/{{feed_id}}/data"
  method: GET
  headers:
    Accept: text/csv
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: text/csv
  body:
    type: text
    content: string
order: 1000
