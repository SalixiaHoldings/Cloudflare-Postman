$kind: http-request
id: b7d04f59-80c1-564b-a2f0-20d58ff0c833
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: accounts-browser-extension-inventories-list
  GET /accounts/{account_id}/browser-extension/config/inventories

  Returns the installed extension inventory for each registered browser
  extension device in the account. Each device reports its inventory
  periodically; this endpoint returns the latest known snapshot per device.

  Results are paginated by cursor. Pass the `cursor` from a previous
  response to fetch the next page. `per_page` controls page size
  (default 50, max 200).


  Authentication support: bearer-only
  Auth declaration SHA-256: cbba9b337efd4dcbaf1248fcfab868d3a542bf11170ae3bcc3755c08bfaa1c27
  Upstream security (operation; OR alternatives, AND within each object): [{"api_token":[]}]
  Selected requirement: {"api_token":[]}
  Use the narrowly scoped api_token; legacy alternatives are not enabled.
url: "{{base_url}}/accounts/{{account_id}}/browser-extension/config/inventories"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: cursor
    value: string
    disabled: true
    description: Opaque cursor from a previous response to fetch the next page.
  - key: per_page
    value: "50"
    disabled: true
    description: Number of results per page (default 50, max 200).
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/List browser extension inventories.resources/examples
order: 1000
