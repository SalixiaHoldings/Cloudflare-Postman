$kind: http-request
id: 8e66e8cb-3d00-53a5-a705-5ac1dde0d0c7
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: pay-per-crawl.patchPPUOperatorConfiguration
  PATCH /accounts/{account_id}/pay-per-use/operator/configuration

  Updates one or more fields of an existing pay-per-use operator configuration. Re-enabling a disabled operator requires completed Stripe onboarding; an already-enabled operator is not affected by later Stripe status changes. A Cloudflare-verified operator's name and default price cannot be changed.

  Authentication support: legacy-only
  Auth declaration SHA-256: a9720528d5568ee0454de8ee346c99dd84fa50a430a5033cc320160dae1660d9
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[]}]
  Selected requirement: {"api_email":[],"api_key":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/pay-per-use/operator/configuration"
method: PATCH
headers:
  Content-Type: application/json
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
body:
  type: json
  content: |-
    {
      "default_price_usd_microcents": 222857500000,
      "enabled": true,
      "name": "string"
    }
auth:
  type: noauth
examples: ./.resources/Update pay-per-use operator configuration.resources/examples
order: 2000
