$kind: http-request
id: e5c1152d-eb57-5bb0-a485-5b6f5d9b924b
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: pay-per-crawl.putPPUOperatorConfiguration
  PUT /accounts/{account_id}/pay-per-use/operator/configuration

  Creates or updates the pay-per-use operator configuration for an account. Creating an enabled configuration or re-enabling a disabled operator requires completed Stripe onboarding; an already-enabled operator is not affected by later Stripe status changes. A Cloudflare-verified operator's name and default price cannot be changed.

  Authentication support: legacy-only
  Auth declaration SHA-256: a9720528d5568ee0454de8ee346c99dd84fa50a430a5033cc320160dae1660d9
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[]}]
  Selected requirement: {"api_email":[],"api_key":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/pay-per-use/operator/configuration"
method: PUT
headers:
  Content-Type: application/json
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
body:
  type: json
  content: |-
    {
      "name": "string",
      "enabled": true,
      "default_price_usd_microcents": 399804000000
    }
auth:
  type: noauth
examples: ./.resources/Set pay-per-use operator configuration.resources/examples
order: 3000
