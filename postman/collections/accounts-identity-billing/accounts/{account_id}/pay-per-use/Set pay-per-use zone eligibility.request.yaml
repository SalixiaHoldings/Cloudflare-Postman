$kind: http-request
id: 6c05229b-9e70-5782-a6e1-258149a20b3a
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: pay-per-crawl.setPPUZonesCanBeEnabled
  PATCH /accounts/{account_id}/pay-per-use/zones_can_be_enabled

  Sets pay-per-use eligibility for a list of account zones. An omitted can_be_enabled value leaves that zone unchanged. Revoking eligibility disables the zone immediately while existing accepted licenses remain active until the start of the next UTC month.

  Authentication support: legacy-only
  Auth declaration SHA-256: a9720528d5568ee0454de8ee346c99dd84fa50a430a5033cc320160dae1660d9
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[]}]
  Selected requirement: {"api_email":[],"api_key":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/pay-per-use/zones_can_be_enabled"
method: PATCH
headers:
  Content-Type: application/json
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
body:
  type: json
  content: |-
    {
      "zones": [
        {
          "id": "{{zones__0__id}}"
        }
      ]
    }
auth:
  type: noauth
examples: ./.resources/Set pay-per-use zone eligibility.resources/examples
order: 5000
