$kind: http-example
id: 9542b552-4a14-5ac9-aab7-50543dc0d9e4
request:
  url: "{{base_url}}/accounts/{{account_id}}/billing/profile/payment-method"
  method: POST
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "client_secret": "{{client_secret}}",
          "intent_type": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        }
      }
order: 1000
