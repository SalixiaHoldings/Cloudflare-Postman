$kind: http-example
id: b6ca4244-e37b-5dff-bf57-3d58f85221bd
name: Invalid request.
request:
  url: "{{base_url}}/accounts/{{account_id}}/one/integrations/{{id}}"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "credentials": {
          "access_token": "{{access_token}}",
          "refresh_token": "{{refresh_token}}"
        }
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: ""
order: 2000
