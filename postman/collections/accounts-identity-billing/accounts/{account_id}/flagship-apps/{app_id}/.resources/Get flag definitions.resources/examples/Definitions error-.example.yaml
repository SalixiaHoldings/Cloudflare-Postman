$kind: http-example
id: ced5e05e-097d-5f71-9be3-1dd0196e3d81
name: Definitions error.
request:
  url: "{{base_url}}/accounts/{{account_id}}/flagship/apps/{{app_id}}/definitions"
  method: GET
  headers:
    - key: If-None-Match
      value: string
      description: Previously returned ETag, or `*`.
    - key: Accept
      value: application/json
response:
  statusCode: 500
  statusText: Internal Server Error
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "error": "string"
      }
order: 6000
