$kind: http-example
id: f7f89de2-14f4-5d64-a42b-99cb776fb08c
name: App not found.
request:
  url: "{{base_url}}/accounts/{{account_id}}/flagship/apps/{{app_id}}/definitions"
  method: GET
  headers:
    - key: If-None-Match
      value: string
      description: Previously returned ETag, or `*`.
    - key: Accept
      value: application/json
response:
  statusCode: 404
  statusText: Not Found
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "error": "string"
      }
order: 5000
