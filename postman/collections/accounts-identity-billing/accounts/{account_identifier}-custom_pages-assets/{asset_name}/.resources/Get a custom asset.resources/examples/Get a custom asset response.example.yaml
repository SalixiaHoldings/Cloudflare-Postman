$kind: http-example
id: b2cca6e7-6cfa-5bdd-b325-45577bd93c52
request:
  url: "{{base_url}}/accounts/{{account_identifier}}/custom_pages/assets/{{asset_name}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
