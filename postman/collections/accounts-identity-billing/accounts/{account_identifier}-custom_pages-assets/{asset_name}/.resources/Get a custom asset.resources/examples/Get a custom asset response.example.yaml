$kind: http-example
id: 7b96b906-c7a6-56e3-abc9-91e782cb1966
request:
  url: "{{base_url}}/accounts/{{account_identifier}}/custom_pages/assets/{{asset_name}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
