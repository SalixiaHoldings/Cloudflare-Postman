$kind: http-example
id: 5c5a73a5-661f-59dc-b77d-e2aa75f52770
request:
  url: "{{base_url}}/zones/{{zone_id}}/page_shield/connections/{{connection_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
