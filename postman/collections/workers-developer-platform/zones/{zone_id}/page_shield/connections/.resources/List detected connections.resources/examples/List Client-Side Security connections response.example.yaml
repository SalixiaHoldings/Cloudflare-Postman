$kind: http-example
id: 673cbb58-92bf-593a-9a91-9a24a11ca173
request:
  url: "{{base_url}}/zones/{{zone_id}}/page_shield/connections?exclude_urls=blog.cloudflare.com,www.example&urls=blog.cloudflare.com,www.example&hosts=blog.cloudflare.com,www.example*,*cloudflare.com&page=string&per_page=100&order_by=first_seen_at&direction=asc&prioritize_malicious=true&exclude_cdn_cgi=true&status=active,inactive&page_url=example.com/page,*/checkout,example.com/*,*checkout*&export=csv"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    exclude_urls: blog.cloudflare.com,www.example
    urls: blog.cloudflare.com,www.example
    hosts: blog.cloudflare.com,www.example*,*cloudflare.com
    page: string
    per_page: "100"
    order_by: first_seen_at
    direction: asc
    prioritize_malicious: "true"
    exclude_cdn_cgi: "true"
    status: active,inactive
    page_url: example.com/page,*/checkout,example.com/*,*checkout*
    export: csv
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ]
      }
order: 1000
