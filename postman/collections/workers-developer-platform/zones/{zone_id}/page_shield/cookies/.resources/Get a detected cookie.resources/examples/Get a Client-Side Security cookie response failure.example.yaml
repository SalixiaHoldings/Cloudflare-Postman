$kind: http-example
id: 6676efbc-e2e3-5da5-9694-8945afba24ca
request:
  url: "{{base_url}}/zones/{{zone_id}}/page_shield/cookies/{{cookie_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [],
        "result": null
      }
order: 2000
