$kind: http-example
id: 4d57a2a0-f012-5620-b897-a234868075dc
request:
  url: "{{base_url}}/zones/{{zone_id}}/page_shield/cookies/{{cookie_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
