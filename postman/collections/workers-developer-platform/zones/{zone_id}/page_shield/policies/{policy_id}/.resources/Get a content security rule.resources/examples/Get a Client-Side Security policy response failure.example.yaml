$kind: http-example
id: a7bf028d-f911-50c7-80ca-ef1fcaed8650
request:
  url: "{{base_url}}/zones/{{zone_id}}/page_shield/policies/{{policy_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [],
        "result": null
      }
order: 2000
