$kind: http-example
id: fe4793b2-05e1-5427-912a-d8aed3549523
name: Attach domain failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/workers/domains"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "hostname": "app.example.com",
        "service": "my-worker",
        "zone_id": "{{zone_id}}",
        "zone_name": "example.com"
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
