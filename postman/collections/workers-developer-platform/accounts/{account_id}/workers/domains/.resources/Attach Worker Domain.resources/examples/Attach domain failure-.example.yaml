$kind: http-example
id: c3d28241-c420-5ef6-a0fb-f7d179bbd6bf
name: Attach domain failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/workers/domains"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "hostname": "app.example.com",
        "service": "my-worker",
        "zone_id": "{{zone_id}}",
        "zone_name": "example.com"
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
