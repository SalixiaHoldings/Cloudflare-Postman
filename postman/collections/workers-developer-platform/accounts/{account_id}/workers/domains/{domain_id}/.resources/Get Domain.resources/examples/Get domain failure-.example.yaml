$kind: http-example
id: 3e610da7-6ef8-55ef-ba67-0c61a002dedb
name: Get domain failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/workers/domains/{{domain_id}}"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
