$kind: http-example
id: 8ccfae84-2a93-58ee-91a8-76dfea21aeef
name: Fetch script content.
request:
  url: "{{base_url}}/accounts/{{account_id}}/workers/scripts/{{script_name}}/content/v2"
  method: GET
  headers:
    Accept: string
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: string
  body:
    type: text
    content: |
      export default {
        async fetch(request, env, ctx) {
          return new Response("Hello, world!");
        }
      };
order: 1000
