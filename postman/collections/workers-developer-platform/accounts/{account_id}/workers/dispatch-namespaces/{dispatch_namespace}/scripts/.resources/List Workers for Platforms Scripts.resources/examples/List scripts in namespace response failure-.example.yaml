$kind: http-example
id: d55a8a88-4fe1-5683-855d-96474ac1352b
name: List scripts in namespace response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/workers/dispatch/namespaces/{{dispatch_namespace}}/scripts"
  method: GET
  headers:
    Accept: application/json
  queryParams:
    - key: tags
      value: production:yes,staging:no
      disabled: true
      description: "Filter scripts by tags. Format: comma-separated list of tag:allowed pairs where allowed is 'yes' or 'no'."
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
