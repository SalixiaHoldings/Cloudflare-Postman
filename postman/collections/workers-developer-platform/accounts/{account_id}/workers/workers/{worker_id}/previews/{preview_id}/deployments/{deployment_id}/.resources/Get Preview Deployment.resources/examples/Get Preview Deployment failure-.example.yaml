$kind: http-example
id: 3ba7e763-aaeb-5f02-bd90-bca339de7943
name: Get Preview Deployment failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/workers/workers/{{worker_id}}/previews/{{preview_id}}/deployments/{{deployment_id}}"
  method: GET
  headers:
    Accept: application/json
  queryParams:
    - key: include
      value: modules
      disabled: true
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
