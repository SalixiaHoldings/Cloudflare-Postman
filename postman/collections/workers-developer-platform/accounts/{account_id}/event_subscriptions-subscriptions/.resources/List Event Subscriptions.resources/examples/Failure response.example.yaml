$kind: http-example
id: 70d13f69-5905-5a30-86c8-b36e6a9d5f9e
request:
  url: "{{base_url}}/accounts/{{account_id}}/event_subscriptions/subscriptions"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: page
      value: "1"
      disabled: true
      description: Page number for pagination
    - key: per_page
      value: "20"
      disabled: true
      description: Number of items per page
    - key: order
      value: name
      disabled: true
      description: Field to sort by
    - key: direction
      value: asc
      disabled: true
      description: Sort direction
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "success": false
      }
order: 2000
