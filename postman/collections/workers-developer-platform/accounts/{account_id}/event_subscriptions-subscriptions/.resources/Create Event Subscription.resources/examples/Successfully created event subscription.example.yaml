$kind: http-example
id: dab947bf-41e2-5d81-aa52-163e4df4e96b
request:
  url: "{{base_url}}/accounts/{{account_id}}/event_subscriptions/subscriptions"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "destination": {
          "type": "queues.queue",
          "queue_id": "{{destination__queue_id}}"
        },
        "enabled": false,
        "events": [
          "string"
        ],
        "name": "string"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        },
        "messages": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        },
        "success": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        },
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
