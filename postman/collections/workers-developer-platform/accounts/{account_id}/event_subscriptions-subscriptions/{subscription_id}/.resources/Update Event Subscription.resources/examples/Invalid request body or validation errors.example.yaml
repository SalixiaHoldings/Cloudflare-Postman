$kind: http-example
id: 38e8f063-3790-56e2-9529-9a3e293b7233
request:
  url: "{{base_url}}/accounts/{{account_id}}/event_subscriptions/subscriptions/{{subscription_id}}"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "destination": {
          "type": "queues.queue",
          "queue_id": "{{destination__queue_id}}"
        },
        "enabled": false,
        "events": [
          "string"
        ],
        "name": "string"
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "success": false
      }
order: 2000
