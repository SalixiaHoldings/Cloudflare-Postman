$kind: http-request
id: 47b22aa5-4b23-5a60-a253-e0400ba93faf
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: start_recording
  POST /accounts/{account_id}/realtime/kit/{app_id}/recordings

  Starts recording a meeting. The meeting can be started by an App admin directly, or a participant with permissions to start a recording, based on the type of authorization used.

  Authentication support: bearer-only
  Auth declaration SHA-256: cbba9b337efd4dcbaf1248fcfab868d3a542bf11170ae3bcc3755c08bfaa1c27
  Upstream security (operation; OR alternatives, AND within each object): [{"api_token":[]}]
  Selected requirement: {"api_token":[]}
  Use the narrowly scoped api_token; legacy alternatives are not enabled.
url: "{{base_url}}/accounts/{{account_id}}/realtime/kit/{{app_id}}/recordings"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "allow_multiple_recordings": false,
      "audio_config": {
        "channel": "stereo",
        "codec": "AAC",
        "export_file": true
      },
      "file_name_prefix": "string",
      "interactive_config": {
        "type": "ID3"
      },
      "max_seconds": 60,
      "meeting_id": "97440c6a-140b-40a9-9499-b23fd7a3868a",
      "realtimekit_bucket_config": {
        "enabled": true
      },
      "video_config": {
        "codec": "H264",
        "export_file": true,
        "height": 720,
        "watermark": {
          "position": "left top",
          "size": {
            "height": 1,
            "width": 1
          },
          "url": "http://example.com"
        },
        "width": 1280
      }
    }
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/Start recording a meeting.resources/examples
order: 2000
