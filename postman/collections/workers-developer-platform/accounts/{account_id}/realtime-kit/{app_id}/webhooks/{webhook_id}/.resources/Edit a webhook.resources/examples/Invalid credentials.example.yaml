$kind: http-example
id: c87ea1f0-47bf-58a8-b1ed-5569b8c77a13
request:
  url: "{{base_url}}/accounts/{{account_id}}/realtime/kit/{{app_id}}/webhooks/{{webhook_id}}"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "name": "test",
        "events": [
          "meeting.started"
        ],
        "url": "https://new-test-url.com"
      }
response:
  statusCode: 401
  statusText: Unauthorized
  headers:
    Content-Type: application/json
  body:
    type: json
    content: '"{\n  \"message\": \"Unauthorized\"\n}\n"'
order: 3000
