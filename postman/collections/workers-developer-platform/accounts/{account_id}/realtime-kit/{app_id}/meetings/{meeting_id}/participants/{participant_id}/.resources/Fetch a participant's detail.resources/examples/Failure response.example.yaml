$kind: http-example
id: 8b085293-dc0f-5c13-aed6-9a0cbcec7990
request:
  url: "{{base_url}}/accounts/{{account_id}}/realtime/kit/{{app_id}}/meetings/{{meeting_id}}/participants/{{participant_id}}"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 500
  statusText: Internal Server Error
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "error": {
          "code": 4455.95646975562,
          "message": "string"
        }
      }
order: 2000
