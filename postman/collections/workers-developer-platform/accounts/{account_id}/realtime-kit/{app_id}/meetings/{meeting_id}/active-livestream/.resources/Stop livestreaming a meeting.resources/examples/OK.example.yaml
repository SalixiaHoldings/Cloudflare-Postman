$kind: http-example
id: 69bc9642-665c-52ab-8394-f4b60672fef5
request:
  url: "{{base_url}}/accounts/{{account_id}}/realtime/kit/{{app_id}}/meetings/{{meeting_id}}/active-livestream/stop"
  method: POST
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "data": {
          "message": "Stopped live stream successfully"
        },
        "success": true
      }
order: 1000
