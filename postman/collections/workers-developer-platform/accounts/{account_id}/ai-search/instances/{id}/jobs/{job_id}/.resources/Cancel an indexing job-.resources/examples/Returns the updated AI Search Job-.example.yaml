$kind: http-example
id: b68a810b-961b-563f-9484-9048a6b97693
name: Returns the updated AI Search Job.
request:
  url: "{{base_url}}/accounts/{{account_id}}/ai-search/instances/{{id}}/jobs/{{job_id}}"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "action": "cancel"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": true,
        "result": {
          "id": "string",
          "source": "schedule",
          "description": "string",
          "end_reason": "string",
          "ended_at": "string",
          "last_seen_at": "string",
          "started_at": "string"
        }
      }
order: 1000
