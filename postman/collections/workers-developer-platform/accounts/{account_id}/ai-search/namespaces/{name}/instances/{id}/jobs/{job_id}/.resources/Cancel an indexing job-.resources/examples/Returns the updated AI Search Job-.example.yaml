$kind: http-example
id: 9f401f42-13c0-5757-a887-301007e78ca4
name: Returns the updated AI Search Job.
request:
  url: "{{base_url}}/accounts/{{account_id}}/ai-search/namespaces/{{name}}/instances/{{id}}/jobs/{{job_id}}"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "action": "cancel"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": true,
        "result": {
          "id": "string",
          "source": "schedule",
          "description": "string",
          "end_reason": "string",
          "ended_at": "string",
          "last_seen_at": "string",
          "started_at": "string"
        }
      }
order: 1000
