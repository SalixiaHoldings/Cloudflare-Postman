$kind: http-request
id: d3c711c5-3b3e-57df-914a-0fbdf225d71f
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: brapi-post_DevtoolsLiveView
  POST /accounts/{account_id}/browser-rendering/devtools/browser/{session_id}/live_view

  Generates time-limited URLs to view a remote browser session. Set `guardrails: { mode: 'readonly' }` to create a view-only link.

  Authentication support: multi-scheme-or-other
  Auth declaration SHA-256: cb039fe92c74f6483aaad6a3be8d90dfc474320bb13b733f4b11d38e19a5507f
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[],"api_token":[]}]
  Selected requirement: {"api_email":[],"api_key":[],"api_token":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/browser-rendering/devtools/browser/{{session_id}}/live_view"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
body:
  type: json
  content: |-
    {
      "expiresInMs": 300000,
      "guardrails": {
        "mode": "readonly"
      },
      "mode": "devtools",
      "targetId": "71"
    }
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/Mint live view URLs for a browser session.resources/examples
order: 4000
