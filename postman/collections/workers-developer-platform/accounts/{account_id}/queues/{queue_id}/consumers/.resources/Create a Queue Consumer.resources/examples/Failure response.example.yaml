$kind: http-example
id: c4cc2286-d215-5efb-9884-675f2ae733ab
request:
  url: "{{base_url}}/accounts/{{account_id}}/queues/{{queue_id}}/consumers"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "type": "worker",
        "script_name": "my-consumer-worker"
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "success": false
      }
order: 2000
