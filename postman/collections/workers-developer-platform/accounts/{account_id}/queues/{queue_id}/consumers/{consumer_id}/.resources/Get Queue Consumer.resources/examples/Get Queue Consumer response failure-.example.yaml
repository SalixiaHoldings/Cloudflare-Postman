$kind: http-example
id: 56f3d9dd-1f35-5948-8d2b-bd7fb50c16c5
name: Get Queue Consumer response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/queues/{{queue_id}}/consumers/{{consumer_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "success": false
      }
order: 2000
