$kind: http-example
id: d116bb2d-ae05-5d48-b959-d59472c7979a
request:
  url: "{{base_url}}/accounts/{{account_id}}/queues/{{queue_id}}"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "jurisdiction": "eu",
        "queue_name": "example-queue"
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "success": false
      }
order: 2000
