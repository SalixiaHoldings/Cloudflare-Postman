$kind: http-example
id: 7689dd01-5bc7-51a1-b734-5b23943b0a69
request:
  url: "{{base_url}}/accounts/{{account_id}}/queues/{{queue_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "success": true
      }
order: 1000
