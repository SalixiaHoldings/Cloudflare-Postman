$kind: http-example
id: e42fe33a-54f9-5d67-8fc9-c8aa35e1217f
request:
  url: "{{base_url}}/accounts/{{account_id}}/queues/{{queue_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "success": true
      }
order: 1000
