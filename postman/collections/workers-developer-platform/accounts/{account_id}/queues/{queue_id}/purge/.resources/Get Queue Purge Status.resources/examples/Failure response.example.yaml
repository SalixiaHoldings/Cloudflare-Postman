$kind: http-example
id: cf42007b-49ed-5bd3-ad44-a1f4af6654e6
request:
  url: "{{base_url}}/accounts/{{account_id}}/queues/{{queue_id}}/purge"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "success": false
      }
order: 2000
