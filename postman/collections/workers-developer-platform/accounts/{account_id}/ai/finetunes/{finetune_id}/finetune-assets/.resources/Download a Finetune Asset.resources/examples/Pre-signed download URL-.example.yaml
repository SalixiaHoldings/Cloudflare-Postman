$kind: http-example
id: d23de894-751c-5da8-9215-0a5b683cbfb2
name: Pre-signed download URL.
request:
  url: "{{base_url}}/accounts/{{account_id}}/ai/finetunes/{{finetune_id}}/finetune-assets/{{file_name}}"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "result": {
          "file_name": "string",
          "file_url": "string"
        }
      }
order: 1000
