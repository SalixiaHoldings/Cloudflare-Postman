$kind: http-example
id: aec1e45d-1862-5e5f-be6c-0271b68e4ab2
request:
  url: "{{base_url}}/accounts/{{account_id}}/cloudforce-one/v2/brand-protection/letter/templates/{{template_id}}"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "id": "string",
        "source": "system",
        "name": "string",
        "category": "string",
        "description": "string",
        "body": "string",
        "createdAt": "string",
        "updatedAt": "string"
      }
order: 1000
