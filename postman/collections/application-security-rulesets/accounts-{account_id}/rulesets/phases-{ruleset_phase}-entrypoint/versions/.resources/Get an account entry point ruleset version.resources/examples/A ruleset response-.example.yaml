$kind: http-example
id: c04acb1a-764e-592b-98cc-59ee933d4d08
name: A ruleset response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/rulesets/phases/{{ruleset_phase}}/entrypoint/versions/{{ruleset_version}}"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        },
        "success": true,
        "errors": [],
        "messages": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
