$kind: http-example
id: b3fe0ebd-408e-5cc4-9a74-a2af06daa116
name: Failed to retrieve submitted abuse report emails.
request:
  url: "{{base_url}}/accounts/{{account_id}}/abuse-reports/submitted/{{report_id}}/emails"
  method: GET
  headers:
    Accept: application/json
  queryParams:
    - key: page
      value: "6305"
      disabled: true
      description: Page number to retrieve (default 1).
    - key: per_page
      value: "6305"
      disabled: true
      description: Number of emails per page (default 20, max 100).
response:
  statusCode: 500
  statusText: Internal Server Error
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: ""
order: 5000
