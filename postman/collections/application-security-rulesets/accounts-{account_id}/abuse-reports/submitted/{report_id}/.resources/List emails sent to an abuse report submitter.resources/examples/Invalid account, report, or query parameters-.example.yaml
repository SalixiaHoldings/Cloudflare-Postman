$kind: http-example
id: a889c993-b648-5824-943e-6dd5f910e32c
name: Invalid account, report, or query parameters.
request:
  url: "{{base_url}}/accounts/{{account_id}}/abuse-reports/submitted/{{report_id}}/emails"
  method: GET
  headers:
    Accept: application/json
  queryParams:
    - key: page
      value: "6305"
      disabled: true
      description: Page number to retrieve (default 1).
    - key: per_page
      value: "6305"
      disabled: true
      description: Number of emails per page (default 20, max 100).
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: ""
order: 2000
