$kind: http-example
id: 7b8bb320-a410-58a2-9ef7-85723f0ec40d
name: Client error.
request:
  url: "{{base_url}}/accounts/{{account_id}}/vuln_scanner/scans"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "target_environment_id": "{{target_environment_id}}",
        "scan_type": "bola",
        "open_api": "{{open_api}}",
        "credential_sets": {
          "owner": "{{credential_sets__owner}}",
          "attacker": "{{credential_sets__attacker}}"
        }
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
