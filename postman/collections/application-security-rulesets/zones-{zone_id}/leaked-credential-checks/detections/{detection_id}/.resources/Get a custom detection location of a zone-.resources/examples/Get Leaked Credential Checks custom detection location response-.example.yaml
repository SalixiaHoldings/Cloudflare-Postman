$kind: http-example
id: e4e988af-6596-5135-9ae5-b6a4241f0e8c
name: Get Leaked Credential Checks custom detection location response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/leaked-credential-checks/detections/{{detection_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
