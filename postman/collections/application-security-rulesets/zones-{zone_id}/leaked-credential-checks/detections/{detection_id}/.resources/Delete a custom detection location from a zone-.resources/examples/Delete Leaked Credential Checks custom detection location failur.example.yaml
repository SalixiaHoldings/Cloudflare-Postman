$kind: http-example
id: 4d25a19e-843e-57d1-8ec8-f0f1a930b5a2
name: Delete Leaked Credential Checks custom detection location failure response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/leaked-credential-checks/detections/{{detection_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 10100,
            "message": "Unknown error occured, please try again"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
