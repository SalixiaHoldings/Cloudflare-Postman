$kind: http-request
id: 3c457fc5-d534-58ed-909a-1d85d2be72ab
name: Update a custom detection location of a zone.
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: waf-product-api-leaked-credentials-update-detection
  PUT /zones/{zone_id}/leaked-credential-checks/detections/{detection_id}

  Update the username and password expressions of an existing detection location, identified by its detection ID. Both expressions are overwritten, so omitting the password expression clears it.

  Authentication support: multi-scheme-or-other
  Auth declaration SHA-256: cb039fe92c74f6483aaad6a3be8d90dfc474320bb13b733f4b11d38e19a5507f
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[],"api_token":[]}]
  Selected requirement: {"api_email":[],"api_key":[],"api_token":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/zones/{{zone_id}}/leaked-credential-checks/detections/{{detection_id}}"
method: PUT
headers:
  Content-Type: application/json
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
body:
  type: json
  content: |-
    {
      "password": "{{password}}",
      "username": "lookup_json_string(http.request.body.raw, \"user\")"
    }
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/Update a custom detection location of a zone-.resources/examples
order: 3000
