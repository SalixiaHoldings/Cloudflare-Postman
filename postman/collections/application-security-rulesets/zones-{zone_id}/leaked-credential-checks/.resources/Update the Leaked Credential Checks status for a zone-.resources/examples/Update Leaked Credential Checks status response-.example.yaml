$kind: http-example
id: ea839c8d-7348-529e-b100-83aabe1d8f03
name: Update Leaked Credential Checks status response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/leaked-credential-checks"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "enabled": true
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
