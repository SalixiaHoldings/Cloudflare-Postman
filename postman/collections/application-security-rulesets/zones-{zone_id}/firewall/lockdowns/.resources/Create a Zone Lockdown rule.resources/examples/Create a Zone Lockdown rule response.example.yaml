$kind: http-example
id: 8d111176-c4fb-5033-8f40-bd5acb39b867
request:
  url: "{{base_url}}/zones/{{zone_id}}/firewall/lockdowns"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "urls": [
          "shop.example.com/*",
          "shop.example.com/*"
        ],
        "configurations": [
          {},
          {}
        ],
        "description": "Prevent multiple login failures to mitigate brute force attacks",
        "paused": false,
        "priority": 5
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
