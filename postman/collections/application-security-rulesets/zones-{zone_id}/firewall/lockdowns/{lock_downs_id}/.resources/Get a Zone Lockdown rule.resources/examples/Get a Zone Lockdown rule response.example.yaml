$kind: http-example
id: bf9f458b-fa80-5f27-a789-aab33bff71f2
request:
  url: "{{base_url}}/zones/{{zone_id}}/firewall/lockdowns/{{lock_downs_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
