$kind: http-example
id: 5b88cb01-3268-52a7-a79c-0f963fe43974
request:
  url: "{{base_url}}/zones/{{zone_id}}/firewall/lockdowns/{{lock_downs_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "id": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        }
      }
order: 1000
