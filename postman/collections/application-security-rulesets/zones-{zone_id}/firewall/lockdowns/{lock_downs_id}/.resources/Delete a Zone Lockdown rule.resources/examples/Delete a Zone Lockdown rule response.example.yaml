$kind: http-example
id: 46727425-55d1-5413-922a-13bece504c0d
request:
  url: "{{base_url}}/zones/{{zone_id}}/firewall/lockdowns/{{lock_downs_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "id": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        }
      }
order: 1000
