$kind: http-example
id: 692972ac-9411-50ef-84c3-766227c030a3
request:
  url: "{{base_url}}/zones/{{zone_id}}/firewall/waf/packages/{{package_id}}"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "action_mode": "challenge",
        "sensitivity": "high"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
