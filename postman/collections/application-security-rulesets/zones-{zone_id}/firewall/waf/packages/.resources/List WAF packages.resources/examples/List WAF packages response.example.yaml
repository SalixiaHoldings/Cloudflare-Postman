$kind: http-example
id: cead6bb7-e5cc-51bb-9cda-a3940f6d5ef6
request:
  url: "{{base_url}}/zones/{{zone_id}}/firewall/waf/packages?page=1&per_page=50&order=name&direction=desc&match=all&name=USER"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    page: "1"
    per_page: "50"
    order: name
    direction: desc
    match: all
    name: USER
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "value": "<Error: Too many levels of nesting to fake this schema>"
      }
order: 1000
