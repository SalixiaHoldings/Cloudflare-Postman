$kind: http-example
id: 3769ce2d-299f-55dc-bc79-c140512c14e2
request:
  url: "{{base_url}}/zones/{{zone_id}}/firewall/waf/overrides?page=1&per_page=50"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    page: "1"
    per_page: "50"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ]
      }
order: 1000
