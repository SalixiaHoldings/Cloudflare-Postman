$kind: http-example
id: 7c76c894-474b-5ebf-8f8b-f5ab01d3d6ee
request:
  url: "{{base_url}}/zones/{{zone_id}}/firewall/ua_rules?page=1&description=abusive&per_page=20&user_agent=Safari&paused=false"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    page: "1"
    description: abusive
    per_page: "20"
    user_agent: Safari
    paused: "false"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ]
      }
order: 1000
