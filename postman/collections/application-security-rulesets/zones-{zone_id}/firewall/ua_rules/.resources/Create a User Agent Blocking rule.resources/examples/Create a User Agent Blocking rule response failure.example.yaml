$kind: http-example
id: d2c9868d-f461-5ed5-8591-47d705dde4f3
request:
  url: "{{base_url}}/zones/{{zone_id}}/firewall/ua_rules"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "mode": "challenge",
        "configuration": {},
        "description": "Prevent multiple login failures to mitigate brute force attacks",
        "paused": false
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
