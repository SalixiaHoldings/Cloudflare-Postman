$kind: http-request
id: c772e341-915d-52a8-826c-8adadf002217
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: api-shield-labels-get-labels
  GET /zones/{zone_id}/api_gateway/labels

  Returns all managed and user-defined labels available for web and API operations in the zone.

  Authentication support: multi-scheme-or-other
  Auth declaration SHA-256: cb039fe92c74f6483aaad6a3be8d90dfc474320bb13b733f4b11d38e19a5507f
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[],"api_token":[]}]
  Selected requirement: {"api_email":[],"api_key":[],"api_token":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/zones/{{zone_id}}/api_gateway/labels"
method: GET
headers:
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
queryParams:
  - key: page
    value: "1"
    disabled: true
    description: Page number of paginated results.
  - key: per_page
    value: "20"
    disabled: true
    description: Maximum number of results per page.
  - key: order
    value: description
    disabled: true
  - key: direction
    value: desc
    disabled: true
  - key: source
    value: user
    disabled: true
    description: Filter for labels with source
  - key: filter
    value: login
    disabled: true
    description: Filter for labels where the name or description matches using substring match
  - key: with_mapped_resource_counts
    value: "true"
    disabled: true
    description: Include `mapped_resources` for each label
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/List operation labels.resources/examples
order: 1000
