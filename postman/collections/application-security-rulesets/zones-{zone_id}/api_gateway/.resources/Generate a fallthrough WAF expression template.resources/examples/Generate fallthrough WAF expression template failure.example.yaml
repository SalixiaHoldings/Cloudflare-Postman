$kind: http-example
id: 3c84f0eb-e984-5b69-9791-c7a367dba2b8
request:
  url: "{{base_url}}/zones/{{zone_id}}/api_gateway/expression-template/fallthrough"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "hosts": [
          "{zone}.domain1.tld",
          "domain2.tld"
        ]
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
