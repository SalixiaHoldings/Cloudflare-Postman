$kind: http-example
id: 1f4e4c1c-9ec6-5e6c-bcbb-519ca3780d62
name: Retrieve information about a specific schema zone response failure
request:
  url: "{{base_url}}/zones/{{zone_id}}/api_gateway/user_schemas/{{schema_id}}?omit_source=false"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: omit_source
      value: "false"
      description: Omit the source-files of schemas and only retrieve their meta-data.
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
