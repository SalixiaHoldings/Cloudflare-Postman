$kind: http-example
id: 39a1c413-4598-50cf-b949-69dabfaabdf1
name: Get a rate limit response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/rate_limits/{{rate_limit_id}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
