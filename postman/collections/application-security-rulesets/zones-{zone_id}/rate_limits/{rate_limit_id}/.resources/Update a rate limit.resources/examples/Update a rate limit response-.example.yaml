$kind: http-example
id: 81bf9543-a36d-50ae-88b3-b1d034fe5fc7
name: Update a rate limit response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/rate_limits/{{rate_limit_id}}"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "match": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        },
        "threshold": 60,
        "period": 900,
        "action": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
