$kind: http-example
id: ee3a122b-a7c1-5e99-8434-bcfcb8088baf
name: Disable Content Scanning response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/content-upload-scan/disable"
  method: POST
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": true,
        "errors": [],
        "messages": [],
        "result": {}
      }
order: 1000
