$kind: http-example
id: 53569a78-0fbf-5eed-a761-ec40decb2765
name: Delete Content Scanning custom expression response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/content-upload-scan/payloads/{{expression_id}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ]
      }
order: 1000
