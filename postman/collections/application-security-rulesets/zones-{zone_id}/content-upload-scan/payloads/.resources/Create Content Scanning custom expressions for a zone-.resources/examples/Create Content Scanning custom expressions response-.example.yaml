$kind: http-example
id: 7c277ded-e363-505c-b0da-9392f55cfd67
name: Create Content Scanning custom expressions response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/content-upload-scan/payloads"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      [
        {
          "payload": "lookup_json_string(http.request.body.raw, \"file\")"
        },
        {
          "payload": "lookup_json_string(http.request.body.raw, \"file\")"
        }
      ]
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ]
      }
order: 1000
