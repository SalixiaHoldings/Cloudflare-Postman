$kind: http-example
id: 09f65d76-c8c4-565e-8e77-98065d3fcccf
request:
  url: "{{base_url}}/accounts/{{account_id}}/billing/profile"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "billing_email": "billing@example.com",
        "preferred_locale": "en-US",
        "secondary_billing_email": "secondary@example.com"
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
