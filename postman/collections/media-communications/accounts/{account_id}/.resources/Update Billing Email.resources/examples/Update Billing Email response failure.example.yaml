$kind: http-example
id: e4864bf3-f1c5-5694-8034-f6a9c4ad8a96
request:
  url: "{{base_url}}/accounts/{{account_id}}/billing/profile"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "billing_email": "billing@example.com",
        "preferred_locale": "en-US",
        "secondary_billing_email": "secondary@example.com"
      }
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
