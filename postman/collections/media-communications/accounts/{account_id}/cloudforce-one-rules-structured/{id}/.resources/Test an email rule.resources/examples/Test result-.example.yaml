$kind: http-example
id: b00e7ec9-0282-52d6-884b-25fdde014463
name: Test result.
request:
  url: "{{base_url}}/accounts/{{account_id}}/cloudforce-one/rules/structured/{{id}}/test"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "sample_json": "string"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "matched": true,
        "rules": [
          "string",
          "string"
        ],
        "error": "string"
      }
order: 1000
