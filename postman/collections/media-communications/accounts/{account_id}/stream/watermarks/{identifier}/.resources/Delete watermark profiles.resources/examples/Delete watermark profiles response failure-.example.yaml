$kind: http-example
id: 09723340-eeb4-5e0c-be36-070e6b492d4c
name: Delete watermark profiles response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/stream/watermarks/{{identifier}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
