$kind: http-example
id: a460a1ba-1615-54b6-b510-557e6d6aa354
name: Watermark profile details response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/stream/watermarks/{{identifier}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
