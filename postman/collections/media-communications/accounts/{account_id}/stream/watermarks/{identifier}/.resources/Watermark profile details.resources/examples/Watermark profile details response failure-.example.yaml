$kind: http-example
id: e125c7dc-c333-5dd8-9dc5-0b405e77ff2b
name: Watermark profile details response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/stream/watermarks/{{identifier}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
