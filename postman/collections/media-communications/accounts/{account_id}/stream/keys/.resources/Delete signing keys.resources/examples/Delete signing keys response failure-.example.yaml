$kind: http-example
id: 44ccd480-f654-524f-b4f8-37f9443b157d
name: Delete signing keys response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/stream/keys/{{identifier}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
