$kind: http-example
id: 29f41477-daf8-5e96-98ec-2b20a2ee7977
name: Delete signing keys response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/stream/keys/{{identifier}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": "ok"
      }
order: 1000
