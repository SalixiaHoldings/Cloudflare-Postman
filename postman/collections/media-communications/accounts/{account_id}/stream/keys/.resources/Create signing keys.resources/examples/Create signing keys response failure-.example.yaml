$kind: http-example
id: 2adc2bc2-64c6-5587-8d5c-efeb649972a4
name: Create signing keys response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/stream/keys"
  method: POST
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
