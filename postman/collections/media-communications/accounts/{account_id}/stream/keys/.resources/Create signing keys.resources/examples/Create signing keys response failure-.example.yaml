$kind: http-example
id: acefb7e9-a7f1-5e9e-8517-92707dacd6ba
name: Create signing keys response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/stream/keys"
  method: POST
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
