$kind: http-example
id: 3355de20-8df9-5ad6-936a-1b3ae07af749
name: Create signing keys response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/stream/keys"
  method: POST
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
