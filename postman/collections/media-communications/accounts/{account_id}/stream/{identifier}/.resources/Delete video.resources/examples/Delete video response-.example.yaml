$kind: http-example
id: 092d2f9a-4f0d-5025-a871-b796242a89b0
name: Delete video response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/stream/{{identifier}}"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: ""
order: 1000
