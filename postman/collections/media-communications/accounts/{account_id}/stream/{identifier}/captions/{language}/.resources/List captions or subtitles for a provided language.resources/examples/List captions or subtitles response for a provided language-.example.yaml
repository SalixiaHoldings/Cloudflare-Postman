$kind: http-example
id: fcc0f4ef-dbd5-5d72-ac38-8c4f6366fa38
name: List captions or subtitles response for a provided language.
request:
  url: "{{base_url}}/accounts/{{account_id}}/stream/{{identifier}}/captions/{{language}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
