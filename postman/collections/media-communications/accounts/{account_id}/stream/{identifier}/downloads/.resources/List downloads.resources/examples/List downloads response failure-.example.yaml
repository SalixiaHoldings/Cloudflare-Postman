$kind: http-example
id: 5fecabd1-e41a-5080-bd36-659fc1ec66ae
name: List downloads response failure.
request:
  url: "{{base_url}}/accounts/{{account_id}}/stream/{{identifier}}/downloads"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "messages": [],
        "result": null
      }
order: 2000
