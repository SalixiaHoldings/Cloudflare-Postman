$kind: http-example
id: 9abdc310-d52e-5af2-ac7c-bc08aa732563
name: List downloads response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/stream/{{identifier}}/downloads"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
