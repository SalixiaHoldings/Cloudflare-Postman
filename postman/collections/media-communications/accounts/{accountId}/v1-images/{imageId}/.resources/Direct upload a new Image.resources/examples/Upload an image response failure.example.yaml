$kind: http-example
id: 4c838a1a-0905-58df-a496-881fc9ae8c94
request:
  url: "{{base_url}}/accounts/{{accountId}}/v1/images/{{imageId}}"
  method: PUT
  headers:
    Content-Type: multipart/form-data
    Accept: application/json
  body:
    type: formdata
    content:
      - type: file
        key: file
        src: ""
        description: An image binary data. Only needed when type is uploading a file.
      - type: text
        key: url
        value: string
        description: A URL to fetch an image from origin. Only needed when type is uploading from a URL.
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "success": false,
        "error": "string"
      }
order: 2000
