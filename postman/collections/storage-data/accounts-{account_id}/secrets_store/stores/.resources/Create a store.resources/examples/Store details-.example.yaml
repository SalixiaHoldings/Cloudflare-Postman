$kind: http-example
id: fa393d5d-5b4e-542d-8080-164f3f9b10f5
name: Store details.
request:
  url: "{{base_url}}/accounts/{{account_id}}/secrets_store/stores"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "name": "service_x_keys"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
