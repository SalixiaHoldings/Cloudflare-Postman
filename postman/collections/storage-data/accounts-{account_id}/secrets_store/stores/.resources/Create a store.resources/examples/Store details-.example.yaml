$kind: http-example
id: f8fb0fd5-9e2b-5c51-844b-397efeece4f2
name: Store details.
request:
  url: "{{base_url}}/accounts/{{account_id}}/secrets_store/stores"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "name": "service_x_keys"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
