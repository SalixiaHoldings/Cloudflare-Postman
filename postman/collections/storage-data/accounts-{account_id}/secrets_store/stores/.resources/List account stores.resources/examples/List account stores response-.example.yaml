$kind: http-example
id: 2cdc66b1-7b8d-5fdc-bcc1-e4bd4921b260
name: List account stores response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/secrets_store/stores"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: direction
      value: desc
      disabled: true
      description: Direction to sort objects.
    - key: page
      value: "2"
      disabled: true
      description: Page number.
    - key: per_page
      value: "20"
      disabled: true
      description: Number of objects to return per page.
    - key: order
      value: created
      disabled: true
      description: Order stores by values in the given field.
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ]
      }
order: 1000
