$kind: http-request
id: 0bbb49aa-6833-528c-b00f-11a158648fcd
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: secrets-store-secrets-list
  GET /accounts/{account_id}/secrets_store/stores/{store_id}/secrets

  Lists all store secrets.

  Authentication support: legacy-only
  Auth declaration SHA-256: a9720528d5568ee0454de8ee346c99dd84fa50a430a5033cc320160dae1660d9
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[]}]
  Selected requirement: {"api_email":[],"api_key":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/secrets_store/stores/{{store_id}}/secrets"
method: GET
headers:
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
queryParams:
  - key: direction
    value: desc
    disabled: true
    description: Direction to sort objects.
  - key: page
    value: "2"
    disabled: true
    description: Page number.
  - key: per_page
    value: "20"
    disabled: true
    description: Number of objects to return per page.
  - key: search
    value: string
    disabled: true
    description: Search secrets using a filter string, filtering across name and comment.
  - key: order
    value: created
    disabled: true
    description: Order secrets by values in the given field.
  - key: scopes
    value: workers,websearch
    disabled: true
    description: Only secrets with the given scopes will be returned.
auth:
  type: noauth
examples: ./.resources/List store secrets.resources/examples
order: 2000
