$kind: http-example
id: 56f0ddbd-d22e-5c83-a01b-095ec1c3159b
request:
  url: "{{base_url}}/accounts/{{account_id}}/slurper/jobs/{{job_id}}/pause"
  method: PUT
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "success": false
      }
order: 3000
