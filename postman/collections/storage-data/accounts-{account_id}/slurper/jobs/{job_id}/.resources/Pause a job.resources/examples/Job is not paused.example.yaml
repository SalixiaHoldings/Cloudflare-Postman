$kind: http-example
id: b02b1574-2937-5dba-916d-3f2115eef74f
request:
  url: "{{base_url}}/accounts/{{account_id}}/slurper/jobs/{{job_id}}/pause"
  method: PUT
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 409
  statusText: Conflict
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "success": false
      }
order: 2000
