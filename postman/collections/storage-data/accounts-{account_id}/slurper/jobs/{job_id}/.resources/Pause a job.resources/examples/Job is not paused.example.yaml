$kind: http-example
id: 659a7016-3673-570b-8178-05d16ed90fab
request:
  url: "{{base_url}}/accounts/{{account_id}}/slurper/jobs/{{job_id}}/pause"
  method: PUT
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 409
  statusText: Conflict
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "code": 7003,
            "message": "No route for the URI"
          }
        ],
        "success": false
      }
order: 2000
