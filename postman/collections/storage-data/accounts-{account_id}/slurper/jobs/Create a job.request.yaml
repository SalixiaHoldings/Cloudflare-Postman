$kind: http-request
id: c44f4150-b1ae-5c19-9eb9-6405e86a1d6d
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: slurper-create-job
  POST /accounts/{account_id}/slurper/jobs

  Creates a new R2 Super Slurper migration job to transfer objects from a source bucket (e.g. S3, GCS, R2) to R2.

  Authentication support: multi-scheme-or-other
  Auth declaration SHA-256: cb039fe92c74f6483aaad6a3be8d90dfc474320bb13b733f4b11d38e19a5507f
  Upstream security (operation; OR alternatives, AND within each object): [{"api_email":[],"api_key":[],"api_token":[]}]
  Selected requirement: {"api_email":[],"api_key":[],"api_token":[]}
  Upstream does not declare a standalone API-token alternative. Configure only the empty credential variables required by this request. Multi-scheme requirements are preserved literally, not treated as alternatives. This is schema metadata, not proof of live API behavior.
url: "{{base_url}}/accounts/{{account_id}}/slurper/jobs"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
  X-Auth-Email: "{{api_email}}"
  X-Auth-Key: "{{api_key}}"
body:
  type: json
  content: |-
    {
      "overwrite": true,
      "target": {
        "vendor": "r2",
        "bucket": "{{target__bucket}}",
        "secret": {
          "accessKeyId": "{{target__secret__accessKeyId}}",
          "secretAccessKey": "{{target__secret__secretAccessKey}}"
        }
      }
    }
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/Create a job.resources/examples
order: 2000
