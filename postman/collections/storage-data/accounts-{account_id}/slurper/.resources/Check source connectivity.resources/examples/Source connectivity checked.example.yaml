$kind: http-example
id: e8d4b21d-77c8-50e4-820f-034d53134e7b
request:
  url: "{{base_url}}/accounts/{{account_id}}/slurper/source/connectivity-precheck"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "vendor": "s3",
        "bucket": "{{bucket}}",
        "secret": {
          "accessKeyId": "{{secret__accessKeyId}}",
          "secretAccessKey": "{{secret__secretAccessKey}}"
        }
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        },
        "messages": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        },
        "success": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        },
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
