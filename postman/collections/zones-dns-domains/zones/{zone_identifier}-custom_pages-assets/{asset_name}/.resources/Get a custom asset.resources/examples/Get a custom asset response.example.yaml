$kind: http-example
id: 2b7abdff-dd1e-51ed-aa9f-18349bde1510
request:
  url: "{{base_url}}/zones/{{zone_identifier}}/custom_pages/assets/{{asset_name}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
