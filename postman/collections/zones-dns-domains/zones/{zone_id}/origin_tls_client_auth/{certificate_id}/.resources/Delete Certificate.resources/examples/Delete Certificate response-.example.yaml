$kind: http-example
id: dc462f58-a32b-58e8-ba67-3db33115abe7
name: Delete Certificate response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/origin_tls_client_auth/{{certificate_id}}"
  method: DELETE
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
