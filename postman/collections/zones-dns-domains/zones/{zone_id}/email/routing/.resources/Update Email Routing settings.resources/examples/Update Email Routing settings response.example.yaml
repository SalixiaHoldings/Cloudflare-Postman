$kind: http-example
id: bb35ed79-10c2-5a5b-a3f3-f43f70029c9b
request:
  url: "{{base_url}}/zones/{{zone_id}}/email/routing"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: |-
      {
        "enabled": true,
        "skip_wizard": true,
        "support_subaddress": true
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
