$kind: http-request
id: 651acc52-f9d3-5c15-b745-0f97857e0eb4
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: zone-cache-settings-change-origin-h2-max-streams-setting
  PATCH /zones/{zone_id}/settings/origin_h2_max_streams

  Origin H2 Max Streams configures the max number of concurrent requests that Cloudflare will send within the same connection when communicating with the origin server, if the origin supports it. Note that if your origin does not support H2 multiplexing, 5xx errors may be observed, particularly 520s. Also note that the default value is `100` for all plan types except Enterprise where it is `1`. `1` means that H2 multiplexing is disabled.

  Authentication support: bearer-alternative
  Auth declaration SHA-256: 4844622a422ead8f8ff95dad7881ac91bb3b64cd78aae19a345e1aaa5456fdd3
  Upstream security (operation; OR alternatives, AND within each object): [{"api_token":[]},{"api_email":[],"api_key":[]}]
  Selected requirement: {"api_token":[]}
  Use the narrowly scoped api_token; legacy alternatives are not enabled.
url: "{{base_url}}/zones/{{zone_id}}/settings/origin_h2_max_streams"
method: PATCH
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "value": 50
    }
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/Change Origin H2 Max Streams Setting.resources/examples
order: 2000
