$kind: http-example
id: 747897db-5fa8-553d-8dcd-db6b99e08f6c
request:
  url: "{{base_url}}/zones/{{zone_id}}/dns_records/{{dns_record_id}}"
  method: DELETE
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "id": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        }
      }
order: 1000
