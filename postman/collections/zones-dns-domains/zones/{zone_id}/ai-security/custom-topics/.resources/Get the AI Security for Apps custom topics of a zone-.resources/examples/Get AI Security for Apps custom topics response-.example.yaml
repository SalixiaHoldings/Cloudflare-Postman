$kind: http-example
id: 33f0027c-1937-5834-ab5d-d2fe303b38d2
name: Get AI Security for Apps custom topics response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/ai-security/custom-topics"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
