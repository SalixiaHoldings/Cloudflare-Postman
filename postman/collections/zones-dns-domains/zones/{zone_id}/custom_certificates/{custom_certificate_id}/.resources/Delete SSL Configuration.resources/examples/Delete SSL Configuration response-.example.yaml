$kind: http-example
id: 90cdfc85-d742-59c6-a7e8-250dea54c842
name: Delete SSL Configuration response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/custom_certificates/{{custom_certificate_id}}"
  method: DELETE
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "id": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        }
      }
order: 1000
