$kind: http-example
id: 3a18eac3-2336-5101-843e-3b86e6b9cf5d
name: Get origin cloud region mapping failure.
request:
  url: "{{base_url}}/zones/{{zone_id}}/cache/origin_cloud_regions/{{origin_ip}}"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "code": 1142,
            "message": "Unable to retrieve origin_public_cloud_region setting value. The zone setting does not exist."
          }
        ],
        "messages": [],
        "result": null,
        "success": false
      }
order: 2000
