$kind: http-example
id: a0901f81-1978-5075-857c-845934144225
name: Delete origin cloud region mapping failure.
request:
  url: "{{base_url}}/zones/{{zone_id}}/cache/origin_cloud_regions/{{origin_ip}}"
  method: DELETE
  headers:
    Accept: application/json
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "code": 1142,
            "message": "Unable to retrieve origin_public_cloud_region setting value. The zone setting does not exist."
          }
        ],
        "messages": [],
        "result": null,
        "success": false
      }
order: 2000
