$kind: http-example
id: c545c4a5-19f0-5c90-ad3c-149e6f24db2c
request:
  url: "{{base_url}}/zones/{{zone_id}}/schema_validation/schemas?page=1&per_page=20&omit_source=false&validation_enabled=false"
  method: GET
  headers:
    Accept: application/json
  queryParams:
    - key: page
      value: "1"
      description: Page number of paginated results.
    - key: per_page
      value: "20"
      description: Maximum number of results per page.
    - key: omit_source
      value: "false"
      description: Omit the source-files of schemas and only retrieve their meta-data.
    - key: validation_enabled
      value: "false"
      description: Filter for enabled schemas
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ]
      }
order: 1000
