$kind: http-example
id: 32dce033-7afd-5c42-bc4e-bef273217b75
request:
  url: "{{base_url}}/zones/{{zone_id}}/available_plans/{{plan_identifier}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
