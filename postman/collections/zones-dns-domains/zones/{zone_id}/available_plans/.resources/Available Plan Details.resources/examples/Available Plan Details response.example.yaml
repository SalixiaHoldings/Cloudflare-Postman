$kind: http-example
id: 1007a216-50cc-5c2f-93b4-135ea466fbda
request:
  url: "{{base_url}}/zones/{{zone_id}}/available_plans/{{plan_identifier}}"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
