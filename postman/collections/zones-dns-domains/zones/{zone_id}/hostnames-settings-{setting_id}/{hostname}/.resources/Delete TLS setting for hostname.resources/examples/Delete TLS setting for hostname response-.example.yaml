$kind: http-example
id: dd30595a-700d-567c-aa86-8210c7e2eaf6
name: Delete TLS setting for hostname response.
request:
  url: "{{base_url}}/zones/{{zone_id}}/hostnames/settings/{{setting_id}}/{{hostname}}"
  method: DELETE
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "value": "<Error: Too many levels of nesting to fake this schema>"
        }
      }
order: 1000
