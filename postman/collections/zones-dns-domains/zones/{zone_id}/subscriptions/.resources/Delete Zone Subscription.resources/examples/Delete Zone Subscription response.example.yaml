$kind: http-example
id: 0e21902d-94fa-5a01-8f6c-6016b8d37506
request:
  url: "{{base_url}}/zones/{{zone_id}}/subscriptions"
  method: DELETE
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "subscription_id": {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        }
      }
order: 1000
