$kind: http-request
id: 9444779b-82cd-52b2-a92c-fbd5011a6088
description: |-
  GENERATED FILE — DO NOT EDIT.

  Upstream operation: domain-intelligence-get-domain-details
  GET /accounts/{account_id}/intel/domain

  Gets security details and statistics about a domain.

  Authentication support: bearer-alternative
  Auth declaration SHA-256: 4844622a422ead8f8ff95dad7881ac91bb3b64cd78aae19a345e1aaa5456fdd3
  Upstream security (operation; OR alternatives, AND within each object): [{"api_token":[]},{"api_email":[],"api_key":[]}]
  Selected requirement: {"api_token":[]}
  Use the narrowly scoped api_token; legacy alternatives are not enabled.
url: "{{base_url}}/accounts/{{account_id}}/intel/domain"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: domain
    value: string
    disabled: true
  - key: skip_dns
    value: "true"
    disabled: true
    description: Skip DNS resolution lookups for faster response.
  - key: skip_ranking
    value: "false"
    disabled: true
    description: |
      Skip the domain ranking lookup for faster responses. Defaults to
      `false` (ranking is included). Set to `true` to opt out — primarily
      used by callers like Cloudflare Radar that need to avoid a
      circular dependency when building the domain details page.
      Note: the bulk endpoint (`/intel/domain/bulk`) uses opposite
      defaults — see `include_ranking` there.
auth:
  type: bearer
  credentials:
    token: "{{api_token}}"
examples: ./.resources/Get Domain Details.resources/examples
order: 1000
