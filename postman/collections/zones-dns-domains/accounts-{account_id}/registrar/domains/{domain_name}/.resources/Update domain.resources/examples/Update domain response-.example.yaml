$kind: http-example
id: d9ae4322-237e-56fb-a3d1-3a00146cf611
name: Update domain response.
request:
  url: "{{base_url}}/accounts/{{account_id}}/registrar/domains/{{domain_name}}"
  method: PUT
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  body:
    type: json
    content: "{}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {}
      }
order: 1000
