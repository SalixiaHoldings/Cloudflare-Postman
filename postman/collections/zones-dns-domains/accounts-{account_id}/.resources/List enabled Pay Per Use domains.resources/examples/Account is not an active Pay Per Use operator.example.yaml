$kind: http-example
id: 6bfb3fc0-a02a-5c7a-983c-b272fbe8aea8
request:
  url: "{{base_url}}/accounts/{{account_id}}/pay-per-use/enabled-domains"
  method: GET
  headers:
    Accept: application/json
    X-Auth-Email: "{{api_email}}"
    X-Auth-Key: "{{api_key}}"
  queryParams:
    - key: cursor
      value: string
      disabled: true
      description: Opaque, account-bound cursor returned by the previous page. Cursors expire after one hour; restart pagination without a cursor after receiving a 400 response for an expired cursor.
    - key: per_page
      value: "100"
      disabled: true
      description: Maximum number of domains to return
response:
  statusCode: 403
  statusText: Forbidden
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "errors": [
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          },
          {
            "value": "<Error: Too many levels of nesting to fake this schema>"
          }
        ],
        "result": "",
        "success": false
      }
order: 4000
